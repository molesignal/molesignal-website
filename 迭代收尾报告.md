# molesignal-website 迭代收尾报告

> 自动迭代引擎于 **2026-06-02** 达成 `READINESS_SCORE: 31/31` 触发 DONE 自停。本报告为收尾盘点：完整性验证、交付内容、补密钥与延后复验清单、上线前步骤。

## 一、总览

| 项 | 值 |
|---|---|
| 就绪分 | **31/31（DONE 自停）** |
| 迭代周期 | 29 轮（今日单日完成，自链连跑 + 部分手动 kick 续跑） |
| 工单 | 28 张全部 closed（种子 25 张 + 引擎自主提需 ISSUE-26/27/28） |
| git | main 干净，无残留 feature 分支，118 提交，尾巴已提交（b7b2d7b） |
| 流水线纪律 | 全程 开发→代码审查→独立 QA 三环不省；feature 分支 + Conventional Commits + QA PASS 才 merge --no-ff |
| 本期评分政策 | 不填任何外部密钥；含 `〔需外部:…〕` 项按「代码就绪 + 缺 env 优雅降级经 QA 客观验证」判 `[x]`（用户决策 2026-06-01） |

## 二、完整性验证（收尾实跑，2026-06-02）

29 轮自动合并到 main 的代码，在 main 上整体复验**全绿**：

| 检查 | 结果 |
|---|---|
| `pnpm typecheck`（tsc --noEmit） | ✅ 0 错 |
| `pnpm lint`（eslint） | ✅ 通过 |
| `pnpm a11y:contrast` | ✅ 26/26 全 WCAG AA（明/暗双主题） |
| `pnpm lint:i18n`（EN↔ZH parity） | ✅ 518=518 完全对齐 |
| `pnpm build`（next build 生产构建） | ✅ 成功（EN+ZH ×10 页 + API 路由 + RSS/OG/robots/sitemap/proxy） |
| 死链 `lint:links` | ✅ 0/33 |
| E2E（终验轮） | ✅ 106 passed 零退化 |

**结论**：AI 自迭代的成果是可上线构建的真代码，非纸面达标。

## 三、交付内容概览（按里程碑）

- **M1 上线核心**：限流可靠化（Upstash 分支 + 内存兜底）、Cloud 候补 / Design Partner 申请真入库（Resend Audiences provider，幂等 + 优雅降级）、11 点位转化埋点接线、隐私/条款真实法务文本（替换 LegalStub）、StatusTag/视觉体系、死链与画饼链接清零。
- **M2 生态接线**：GitHub stats/贡献者墙/changelog 真实拉取 + 三兜底态（无 token/无 release 不伪造数字）、changelog RSS 同源、社区入口 config-driven、表单数据导出运营 runbook。
- **M3 决策与终验**：Blog 取稳妥默认 EN-only（友好提示 + 无死链）、CrossSignalDemo 保持样本（reduce-motion 守卫）、全站可信度终验 + 转化埋点闭环（降级验法）。

详见 `READINESS.md` 逐项的 QA 取证记录。

## 四、⚠️ 补密钥 + 延后真实外部联调复验清单

下列项本期按「代码就绪 + 优雅降级」判 `[x]`，**真实外部联调延后到补密钥后复验**（不影响本期 DONE，但**真上线前必须补做**）：

| 需补的外部依赖 | 解锁的就绪项 | 补后须复验 |
|---|---|---|
| `RESEND_API_KEY` + `RESEND_CLOUD_AUDIENCE_ID` | Cloud 候补真入库（ISSUE-8） | 合法 email 真出现在 Resend audience + founders 收通知 |
| `RESEND_API_KEY` + `RESEND_PARTNER_AUDIENCE_ID` | Design Partner 持久化（ISSUE-9） | 5 字段入库 + replyTo 通知 |
| Resend 账号可访问 Audiences | 表单数据导出 runbook（ISSUE-19） | Dashboard 真实导出 CSV 可用 |
| `UPSTASH_REDIS_REST_URL/TOKEN` | 限流可靠化（ISSUE-1） | 真实跨实例限流 429 + Retry-After |
| `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` | 埋点 11 点位 / 转化闭环（ISSUE-2/28） | Plausible 真收到事件、48h 内 ≥1 真实转化 |
| `GITHUB_TOKEN` + 仓库公开/有 Release | stats/贡献者墙/changelog（ISSUE-7/17/21） | 真实数据切换（缺时已验兜底态） |
| 真实 Discord 邀请链接（`NEXT_PUBLIC_DISCORD_INVITE_URL`） | 社区入口（ISSUE-18） | 真实外链无死链（缺时诚实禁用态） |
| 真实 helm repo / binary release（`NEXT_PUBLIC_QUICKSTART_ARTIFACTS_READY=true`） | QuickStart 切换（ISSUE-20） | 移除 v1.0 notice、命令冒烟 |
| 法务文本定稿（律师/模板审核） | 隐私/条款（ISSUE-4） | 法务复审当前模板自拟文本 |

## 五、补密钥清单（`.env.local`）

模板见 `.env.local.example`。`cp .env.local.example .env.local` 后填：

```
# Resend（候补/申请入库 + 通知）
RESEND_API_KEY=
RESEND_CLOUD_AUDIENCE_ID=
RESEND_PARTNER_AUDIENCE_ID=
FOUNDERS_EMAIL=                       # 缺省有内置默认

# Upstash Redis（可靠限流；缺则回退内存 Map）
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Plausible（转化埋点）
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=        # 例 molesignal.com

# GitHub（stats/贡献者/changelog；缺则静态兜底）
GITHUB_TOKEN=
```

可选功能开关（外部产物就绪后再开）：
```
NEXT_PUBLIC_DISCORD_INVITE_URL=          # Discord 就绪后填，自动点亮社区入口
NEXT_PUBLIC_QUICKSTART_ARTIFACTS_READY=  # helm/binary 就绪后设 true，移除 v1.0 notice
```

## 六、上线前步骤建议

1. **补密钥** → 填 `.env.local`（或 Vercel 环境变量）。
2. **复验外部联调** → 对第四节清单逐项跑真实联调复验（Resend 入库、Upstash 限流、Plausible 上报、GitHub 真实数据）。
3. **法务定稿** → 隐私/条款模板文本交律师或正式模板复审。
4. **部署 Vercel** → 见 `DEPLOYMENT.md`（已有部署文档）。
5. **观测转化** → 按 LAUNCH.md 目标，上线 48h 内观测 ≥1 条真实转化事件，闭环验证埋点。

## 七、后续迭代

引擎已自停（`engine/DONE` 旗标置位，launchd 后续触发自动跳过）。如需继续迭代：
- 删 `engine/DONE` 重新激活引擎；或
- `/迭代 molesignal-website <新需求>` 走单需求闭环；或
- `/修bug molesignal-website <描述>` 修复闭环。

非阻断 follow-up（READINESS 中记录的待收口小项）：sitemap.ts:87 changelog 锚点未调 versionAnchor()、lib/email.ts 错误日志 body 未截断、蜜罐 silent-200 死代码（与 cloud-waitlist 同构、安全保证仍成立）——均不影响上线，可日后顺手清。
