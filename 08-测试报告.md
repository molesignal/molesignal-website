# 08 — 测试报告（molesignal-website）

> 本文件由质量环写入。qa-tester 负责测试策略/用例；qa-automation 负责自动化执行与运行结果（下方"自动化测试"小节）。

---

## 自动化测试

### ISSUE-1 [T11] 可靠限流后端（Upstash + 内存兜底）— 2026-06-02

**结论：VERDICT PASS**（本期 AC1–AC7 全过；AC8 真实跨实例延后，无密钥不验，不阻塞 DONE）。

#### 1. 测试范围
- **接口（HTTP E2E）**：`POST /api/cloud-waitlist`（10/h）、`POST /api/design-partner`（5/h）的限流行为——阈值、429 响应体/`Retry-After` 头、两表单 key 独立计数。
- **降级路径**：无 `UPSTASH_*` env → 内存兜底（本期默认生产路径）正常服务 + fallback 警告只打一次（AC2）。
- **容错路径**：配置 `UPSTASH_*` 但后端不可达 → fail-open 放行、不返回 5xx（AC6）。
- **安全（附加）**：H1 — 客户端伪造 `X-Forwarded-For` 最左段不能轮换绕过限流。
- **未覆盖**：AC8 真实 Upstash 跨实例共享计数（本期无密钥，延后复验）；AC7 构建门禁属 CI 域，由开发/审查环跑（本环按"运行时验证"口径不复跑）。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0 / Next.js 16.2.6（Turbopack）。
- 启动：`PORT=3199 pnpm dev -p 3199`（Ready 245ms）。`RESEND_API_KEY` 未配 → 邮件发送被跳过（`{skipped:true}`），有效提交无外部副作用。
- 驱动：`curl` 真实 HTTP 请求，捕获状态码 + `Retry-After` 头 + 服务端日志。
- fail-open 子场景：以 `UPSTASH_REDIS_REST_URL=https://127.0.0.1:1`、`UPSTASH_REDIS_REST_TOKEN=bogus-token` 重启实例。

#### 3. 结果汇总

| AC | 验证点 | 运行结果 | 证据 |
|----|--------|----------|------|
| AC1 | async 接线（await 生效） | ✅ 超阈值由 200 切 429，证明 await rateLimit 生效 | 见 AC3 |
| AC3 | 阈值不变、key 独立 | ✅ dp 5×200 后 429；cloud 10×200 后 429；两表单互不影响 | 下方日志 |
| AC4 | 429 + Retry-After 非负整数 | ✅ `Retry-After: 3600`，响应体结构未变 | 下方日志 |
| AC5 | 可复现 (max+1)→429 | ✅ 两表单各稳定复现 | 下方日志 |
| AC2 | 缺 env 降级 + warn 一次 | ✅ ~26 请求仅 1 条 fallback 警告，不崩 | 日志 grep -c =1 |
| AC6 | fail-open 不返回 5xx | ✅ Upstash 不可达，8 次（超 max）全 200，每请求 1 条 failing-open 警告 | 下方日志 |
| H1 | XFF 最左段不可绕过 | ✅ 轮换最左段、固定尾段 → 第 6 次起 429 | 下方日志 |
| AC7 | 构建门禁 | （CI 域，开发/审查环已跑，本环不复跑） | — |
| AC8 | 真实跨实例 | 延后（无密钥） | — |

**用例清单与结果**（关键运行证据）：

```
# design-partner (max=5/h)
req#1..5 -> HTTP 200 {"ok":true}
req#6   -> HTTP 429  Retry-After=3600  {"error":"Too many requests. Try again later."}
req#7   -> HTTP 429  Retry-After=3600

# cloud-waitlist (max=10/h)  —— 独立计数（design-partner 已耗尽不影响）
req#1..10 -> HTTP 200
req#11/12 -> HTTP 429  Retry-After=3600

# AC2 内存兜底 warn-once
grep -c "falling back to" server.log = 1   （全程约 26 次请求）

# AC6 fail-open（bogus UPSTASH_*，host 不可达）
req#1..8 -> HTTP 200      （全部放行，超 max 5 仍不限、无 5xx）
grep -c "failing open" log = 8 ; grep -c "falling back to" log = 0
日志: [rate-limit] Upstash limit() failed — failing open (request allowed): TypeError: fetch failed

# H1 安全探针（轮换 XFF 最左段，固定尾段 7.7.7.7）
XFF "10.0.0.1, 7.7.7.7" .. "10.0.0.5, 7.7.7.7" -> 200 ×5
XFF "10.0.0.6, 7.7.7.7"                          -> 429   （最左段轮换无法绕过）
```

#### 4. 缺陷清单
无。阻断级缺陷 0。

#### 5. 回归套件位置与重跑命令
- **单元/脚本级**（内存兜底 + fail-open 子进程断言）：`pnpm test:rate-limit`（`scripts/check-rate-limit.ts`）。
- **HTTP 层手动驱动**（重跑步骤）：
  1. `PORT=3199 pnpm dev -p 3199`
  2. 对 `/api/design-partner` 连发 6 次有效 POST → 期望 5×200 后 429（`Retry-After` 非负整数）。
  3. 对 `/api/cloud-waitlist` 连发 11 次 → 期望 10×200 后 429。
  4. fail-open：带 bogus `UPSTASH_*` 重启，连发超阈值 → 期望全 200 + 日志 "failing open"。

#### 6. 放行建议
本期必过项 AC1–AC7 全部通过（运行时真实验证），无阻断缺陷。**建议合并放行**。AC8（真实 Upstash 跨实例）待补 `UPSTASH_*` 密钥后复验，按用户决策不阻塞本期 DONE。

---

### ISSUE-3 [T04] 死链/画饼治理 — 运行时验证（qa-automation, 2026-06-02）

被验证工单：`backlog/ISSUE-3.md`（P0·light 道）。分支 `feature/ISSUE-3`（commits 80668ae / 17eddcb / a8d9560）。

#### 1. 测试范围
- **链接回归（lint:links）**：全站 33 个 URL（EN+ZH 全页 + sitemap/robots/rss/og）真实 HTTP 抓取，断言 2xx/3xx。
- **i18n 双语 parity（lint:i18n）**：messages/en.json ↔ zh.json 键集合一致性。
- **HTML 渲染断言（curl）**：docs.molesignal.io 死链清除、无 `href="#"` 占位、`#install` 锚点目标存在、helm/binary v1.0 notice 双语渲染。
- **交互式 E2E（Playwright + Chromium，真实浏览器）**：Discord/Twitter 禁用态不可点、Resources 下拉无 Docs、Download → /start#install 真实跳转并滚动到安装区、helm/binary tab 点击后 v1 notice 可见而 docker tab 不显示。

#### 2. 执行环境与启动方式
- Node v23.6.1（`~/.nvm/versions/node/v23.6.1/bin` 需入 PATH）、pnpm 11.5.0。
- `pnpm dev`（Next.js 16.2.6 Turbopack，:3000）。**注意**：首跑撞到全站 500（`SyntaxError: Unexpected non-whitespace ... JSON`），根因是仓库内残留的 `next build` 产物 `.next/` 与 `next dev` Turbopack 布局冲突——属环境脏缓存，非 T04 代码缺陷；`rm -rf .next` 重启后全部 200。**建议**：`.gitignore` 已含 `.next`，但工作树中存在历史产物，devops/引擎在 dev 前应清缓存。
- E2E：`node tests/e2e/issue3-t04-deadlinks.mjs`（standalone Playwright，直驱已装 chromium，对 dev :3000）。

#### 3. 结果汇总
| 检查 | 结果 |
|---|---|
| `pnpm lint:links` | 33 URL，**32 ok / 1 FAIL**（`/opengraph-image` 404）→ 见缺陷①，**非 T04 引入**；T04 新增/改动的 4 条内链 `/roadmap`、`/changelog`、`/blog`、`/start#install` 全 200（EN+ZH） |
| `pnpm lint:i18n` | **PASS** — EN 466 / ZH 466 键，parity OK（AC5 ✓） |
| HTML 渲染断言 | docs.molesignal.io = 0、`href="#"` = 0、`#install` 目标存在、v1Notice EN+ZH 均渲染 — 全 ✓ |
| Playwright E2E（13 断言） | **13/13 PASS**（AC1/AC2/AC3/AC4 全覆盖） |

E2E 13 断言明细（全 PASS）：无 docs 锚点 / 无 href="#" / footer Discord 非锚点 / footer Discord aria-disabled / footer Twitter 非锚点 / community-callout Discord 卡禁用 / Resources 菜单含 Download / Resources 菜单无 Docs / Download 跳转 `/start#install`（实测 URL=`http://localhost:3000/start#install`）/ `#install` 锚点滚入视口（y=777）/ helm tab 显 v1 notice / binary tab 显 v1 notice / docker tab 不显 v1 notice。

AC 逐条对照：① Footer/TopNav 移除 docs ✓ | ② Discord/Twitter 禁用态非 href# ✓（footer+community 双处实测） | ③ Download coming-soon→`/start#install` 真实跳转+滚动 ✓ | ④ helm/binary v1.0 target notice ✓ | ⑤ messages 双语同步 466/466 ✓ | ⑥ lint:links 全 2xx：**T04 范围内链全绿**，仅 1 条预存且与 T04 无关的 og 路由 404（见缺陷①）。

#### 4. 缺陷清单
- **缺陷①〔非阻断·非本工单·建议另立工单〕** `/opengraph-image` 在 dev 下 404。
  - 复现：`pnpm dev` → `curl -i http://localhost:3000/opengraph-image` → 404。
  - 预期 vs 实际：lint:links seed 期望 2xx；实际 404。
  - 责任模块：`app/opengraph-image.tsx`（OG 元数据路由）。
  - **判定不阻断 T04**：① 该文件与 `main` 完全一致（`git diff main..HEAD` 为空），ISSUE-3 未触碰；② seed 早在 `main` 的 check-links.ts 即存在；③ 属预存问题（疑似 metadata 图像路由在 Turbopack dev 下不走该路径，需 `next build`/`next start` 验证）。建议 devops/T12 单独跟进 OG 路由产线验证，不应回流 ISSUE-3。
- **观察项〔非缺陷·脚本路径 bug〕** `pnpm lint:quickstart` 失败 4/9，全因脚本去 `workspace/README.md`（项目父目录）读取 README 而文件不在那；网站侧 5 项全 ok。与 T04 无关（T04 未改 quickstart 命令/README），且不在 ISSUE-3 AC 内。建议修 `scripts/check-quickstart-sync.ts` 的 ROOT 路径。

#### 5. 回归套件位置与重跑命令
- 链接/i18n：`pnpm lint:links`（需 `pnpm dev` 在 :3000）、`pnpm lint:i18n`。
- T04 交互 E2E：`node tests/e2e/issue3-t04-deadlinks.mjs`（需 `pnpm dev` 在 :3000；产出截图 `test-results/issue3-t04-binary-notice.png`）。
- 重跑前置：PATH 含 node v23.6.1；若全站 500 先 `rm -rf .next` 再 `pnpm dev`。

#### 6. 放行建议
**VERDICT: PASS** — ISSUE-3 全部 6 条 AC 实测通过（lint:i18n 绿、T04 范围内链全 2xx、13/13 浏览器 E2E 绿、HTML 断言全绿、截图佐证）。唯一 lint:links 失败项 `/opengraph-image` 经核为**预存且与 T04 无关**，不阻断本工单放行；另建议另立工单跟进 OG 路由与 lint:quickstart 脚本路径。

---

### ISSUE-4 [T06] 隐私/条款真实法务文本 — 运行时验证（qa-automation, 2026-06-02）

**结论：VERDICT PASS** — AC1–AC8 全部实测通过；唯一 lint:links 失败 `/opengraph-image` 404 为预存项（ISSUE-3/T04 已记录），非本页链接、与 T06 无关，不阻断放行。

#### 1. 测试范围
- **内容 E2E（浏览器，真 Chromium）**：`/privacy /terms /zh/privacy /zh/terms` 四页——六大隐私覆盖点文本、terms 独立真实条款、EN/ZH 双语无残英/无占位、日期、链接锚点、主题切换不崩。
- **内容断言（HTTP）**：curl 拉取四页渲染 HTML，逐条 grep 覆盖点 + 无 `stub` 残留。
- **链接门禁**：`lint:links` 全站爬（生产构建）；apache 外链独立 2xx 复核。
- **静态门禁**：`typecheck` / `lint:i18n`（parity）/ `a11y:contrast` / `check` / `build`。
- **回归**：全量 E2E 套件 20/20（14 既有 analytics-funnel + 6 新增 legal），确认本分支 `playwright.config.ts` 改动（reducedMotion 移入 contextOptions）未回归。
- **未覆盖/不适用**：无外部密钥即可全程验证（纯文案/i18n，不依赖 Resend/Plausible/Upstash env）。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0 / Next.js 16.2.6。PATH 需含 `~/.nvm/versions/node/v23.6.1/bin`。
- HTTP 断言：`PORT=3137 pnpm build && PORT=3137 pnpm start`（生产构建，SSG 预渲染四页）→ curl。
- E2E：`pnpm exec playwright test`（config 自带 webServer，`next start -p 3210` 生产构建）。
- 链接：`SITE=http://localhost:3137 pnpm lint:links`（须打生产构建；dev/Turbopack 并发爬会进 JSON.parse 500 坏态，见“观察项”）。

#### 3. 结果汇总

| AC | 验证点 | 运行结果 | 证据 |
|----|--------|----------|------|
| AC1 | 隐私页六覆盖点（字段/Plausible无cookie不存IP不跨站DNT/Resend/IP限流/localStorage非追踪/founders联系+不出售） | ✅ EN 16 项 grep 全 PASS + E2E 断言全绿；无 stub | HTML 断言 + E2E#1 |
| AC2 | 条款页独立真实（as-is/Apache-2.0/可接受使用/无担保/可更新） | ✅ 与 privacy 彻底分开、stub 已删 | HTML 断言 + E2E#3 |
| AC3 | EN/ZH parity + ZH 对等翻译非占位 | ✅ EN/ZH 各 504 键 parity OK；legal 数组同构（privacy 8/8、terms 5/5）；ZH 无残英 | `lint:i18n` 绿 + E2E#4/#5 |
| AC4 | 两页日期=2026-06-02 | ✅ 四页均含 `2026-06-02` | HTML 断言 + E2E |
| AC5 | 文本忠于事实（F1 Resend仅传输不入名单 / F2 IP+时间戳+字段作通知邮件留存 / F3 服务器日志 / 无虚假cookie/出售声明） | ✅ 文案与 security-auditor F1–F4 口径一致，逐条在页面文本核到 | messages dump + HTML 断言 |
| AC6 | 页内链接 2xx（mailto/apache/站内） | ✅ mailto+apache 为真实 `<a href>`；apache 外链 200；四法务页 + /cloud /design-partner 全 2xx | `lint:links`（生产）+ E2E 锚点断言 |
| AC7 | check+build 通过、四态渲染无崩 | ✅ `pnpm check` exit 0、`pnpm build` exit 0、E2E 主题切换无 pageerror | check/build 日志 + E2E#6 |
| AC8 | 免责声明就位 | ✅ disclaimer「plain-language summary, not formal legal advice…email founders@」渲染于两页 | messages dump + HTML |

#### 4. 用例清单与结果
- HTML 内容断言：EN privacy 16/16 PASS、EN terms 8/8、ZH privacy 7/7、ZH terms（担保/Apache/日期）PASS、四页均无 `placeholder for v1 launch`。
- 链接锚点：四页 `mailto:founders@molesignal.io` 真实锚点；两 terms 页 `https://www.apache.org/licenses/LICENSE-2.0` 真实锚点；无裸 URL 文本。
- E2E：6/6 新增 legal 用例绿；全量 20/20 绿。
- 门禁：typecheck exit 0、lint:i18n 504/504 OK、a11y:contrast 26/26 AA、check exit 0、build exit 0。
- lint:links（生产构建）：32/33 ok，唯一 FAIL=`/opengraph-image 404`（预存、非本页链接）。

#### 5. 缺陷清单
- **无阻断级缺陷。**
- 〔观察项·非阻断·dev-only〕`pnpm dev`（Turbopack）下被 `lint:links` 并发爬时，全站路由进入 `SyntaxError: Unexpected non-whitespace character after JSON`（proxy/middleware）坏态返 500，且不自愈。**生产构建（`build`+`start`）下全程 200、E2E 全绿，不可复现**——判为 dev/Turbopack HMR 并发坏态，与本工单内容无关，不影响上线。建议另立工单排查 dev 体验（修复前：dev 出现全站 500 时 `rm -rf .next` 重启；链接门禁一律打生产构建跑）。
- 〔预存·非本工单〕`/opengraph-image` 404：ISSUE-3/T04 已记录，留对应工单处理。

#### 6. 回归套件位置与重跑命令
- 新增 E2E：`tests/e2e/issue4-legal.spec.ts`（6 用例，含 EN/ZH×privacy/terms 覆盖点、链接锚点、主题切换韧性）。
- 重跑：`export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"`
  - 内容+回归：`pnpm exec playwright test`（或仅本工单 `… playwright test tests/e2e/issue4-legal.spec.ts`）
  - 门禁：`pnpm check && pnpm build`
  - 链接（须生产构建）：`PORT=3137 pnpm start &` → `SITE=http://localhost:3137 pnpm lint:links`

#### 7. 放行建议
**VERDICT: PASS** — ISSUE-4 八条 AC 全部实测通过（内容六覆盖点齐、双语 parity 504/504、日期准、AC5 文案与 security-auditor F1–F4 纠偏一致、链接锚点 2xx、check+build 绿、20/20 E2E 绿）。建议合并回 main。dev-only 全站 500 观察项与 `/opengraph-image` 404 均为既有/环境项，不阻断本 P0 内容工单。

---

## 代码审查

### ISSUE-2 [T05] 转化漏斗埋点接线（11 点位）— code-reviewer, 2026-06-02

**审查范围**：`git diff main...feature/ISSUE-2`（24 文件，+379/-12），含 `components/analytics/analytics-clicks.tsx`（新增委托式点击追踪器）、7 个客户端组件直调 `track()`、11 处服务端组件 `data-analytics-*` 标注、`lib/analytics.ts` docstring 修正。

**结论：0 必改，无阻断缺陷。建议放行到 QA 自动化验证。**

#### 审查口径与验证
- **构建/静态门禁**（本地实跑，Node v23.6.1）：`pnpm typecheck` ✅ 0 error；`pnpm lint` ✅ 0 error / 5 warning（5 条均为 `theme-toggle.tsx:37` 与 `toc.tsx:48` 的既有 `set-state-in-effect`，与本次埋点改动无关——本次 theme-toggle 改动在 `:78`）。与开发记录自测一致。
- **AC1 全覆盖 + 无多余**：11 事件名全部接线，命名全为 `snake_case` 动宾、与 06§4.4 逐条一致。证据：`track("…")` 直调 7 个事件（`demo_tab_switch`/`cost_calculator_interact`/`quickstart_copy`/`waitlist_submit`/`design_partner_submit`/`locale_switch`/`theme_switch`）+ `data-analytics-event="…"` 标注 4 个事件（`cta_click`/`compare_table_expand`/`github_star_click`/`rss_subscribe`）。无 kebab、无 §4.4 之外的事件。
- **AC3 表单时序（核心风险点）**：✅ 正确。`cloud-waitlist-form.tsx:64` / `design-partner-form.tsx:66` 均在 `if (!res.ok) throw` 之后、`setSubmitted(true)` 同分支调用，且无 props。zod 失败被 react-hook-form 拦在 onSubmit 外、429/5xx/网络落 catch、蜜罐分支在 fetch 前 `return` 且无 `track()`（遵架构裁决，bot 不污染漏斗）。三条反例均不触发。
- **AC4 去重**：✅ `cost-calculator.tsx` `useRef(false)` 守卫，首次任一滑块拖动发一次；`cross-signal-demo.tsx:53` 仅 `key !== active` 时发，且自动巡演走 `setActive`（:36-44）不经 `pick`，故巡演不误发——已核对源码确认。
- **AC5 无 PII**：✅ 两表单事件无 props；`locale_switch.page` 用 i18n 的 locale-strip pathname；`cta_click.destination` 为静态路由/`mailto:founders`（非用户数据）。无邮箱/姓名/IP。
- **委托追踪器正确性**（`analytics-clicks.tsx`）：✅ capture 阶段监听早于 Next `<Link>` 导航，事件不丢；`closest("[data-analytics-event]")` 正确处理图标/`<span>` 子元素点击；`JSON.parse` 包 try/catch 容错；`track()` 缺域名安全 no-op。单一 document 监听挂在 `layout.tsx`，跨路由不重复挂载。**无双发风险**：`ThemeToggle`/`LocaleSwitcher`/demo 按钮/表单提交均无 `data-analytics-event` 且不嵌套于带该属性的祖先，委托器对其返回 null。
- **契约**：✅ 不动 API 出入参、不动数据模型、不加依赖。唯一组件签名变更是 `CodeBlock`/`CodeBlockCopy` 新增可选 `analytics` prop（客户端内部，非对外契约），向后兼容（未传则不发）。与架构评估一致。
- **提交规范**：✅ `feat(ISSUE-2): 转化漏斗埋点接线(11点位)`，符合 Conventional Commits。

#### 发现清单

| # | 位置 | 类型 | 级别 | 说明 / 建议 |
|---|------|------|------|------|
| 1 | `analytics-clicks.tsx:30` vs `locale-switcher.tsx:39` | 坏味道（数据一致性） | 建议改 | `source_page`/`page` 维度口径不一致：委托器用 `window.location.pathname`（`localePrefix:"as-needed"` → EN `/why`、ZH `/zh/why`），而 `locale_switch.page` 用 i18n `usePathname()`（locale-strip → 两语均 `/why`）。结果 `cta_click` 等的 `source_page` 会**按语言分裂漏斗**，与 `locale_switch.page` 对不上。建议委托器也用 locale-strip 的 path（或反之统一），具体取舍交 data-analyst 定。非功能 bug，不阻断本期。 |
| 2 | `theme-toggle.tsx:78` | 设计权衡 | 可选 | `theme_switch.theme` 仅发 `light`/`dark`（toggle 不暴露 `system`），§4.4 枚举含 `system` 但永不触发。data-analyst 已确认为预期（UI 无 system 选项），记录备查。 |
| 3 | CTA 边界（开发记录已列） | 风格/判断 | 可选 | 内联文字链、start 页 next-steps 卡片、404/500 恢复按钮刻意不计 `cta_click`。口径一致且可判定；若 QA/data-analyst 认为某内联链应计入，补 `data-analytics-event` 即可（零结构改动）。 |

#### 放行建议
正确性、契约、命名、时序、去重、PII 六项均达标，typecheck/lint/build 三关本地实跑通过，无必改项。**建议放行到 qa-automation 做运行时验证**（spy `window.plausible`：11 事件触发、props、表单 2xx/4xx/蜜罐三路径、去重）。发现 #1（source_page 语言分裂）建议在本期或紧邻迭代由 data-analyst 拍板统一口径——不阻断 DONE，但会影响漏斗数据质量，宜尽早。

---

### ISSUE-2 [T05] 转化漏斗埋点接线(11点位) — 运行时 E2E 验证（qa-automation, 2026-06-02）

**结论：VERDICT PASS**（AC1–AC7 全绿；AC8 真实 Plausible 上报本期无域名延后，不阻断 DONE）。

#### 1. 测试范围
- **真实浏览器 E2E**（Playwright + Chromium，对**生产构建** `next start` 实跑）：在每次导航前注入 `window.plausible` 录制桩（事件落 `sessionStorage`，跨同标签页客户端导航存活），逐一触发 11 个交互并断言 **事件名 + props + 触发时序**。
- 覆盖 §4.4 全部 11 事件；重点压 **AC3 表单时序三路径**（2xx 触发 / 4xx·5xx 不触发 / 蜜罐不触发）与 **AC4 去重**（demo 同 tab 重复点、计算器每挂载一次）。
- **静态全量核对**：8 处 `track()` 直调 + 27 处 `data-analytics-event` 标注，11 事件名逐条比对 06§4.4，全 `snake_case`、无遗漏无多余。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0；`pnpm build` 产物，Playwright `webServer` 自动起 `next start -p 3210`（`reuseExistingServer`），跑完自动回收（已确认端口 3210 无残留进程）。
- 未配 `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` → `PlausibleScript` 返回 null（不注入真实脚本），录制桩存活，符合本期判过政策（spy/mock 客观验证即判 `[x]`）。
- 重跑命令：
  ```bash
  export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
  cd <repo>
  pnpm build && pnpm exec playwright test          # 回归套件（14 用例）
  ```
  套件位置：`playwright.config.ts` + `tests/e2e/analytics-funnel.spec.ts`。

#### 3. 结果汇总：14/14 通过，0 失败

| # | 用例 | 事件 / 验收点 | 结果 |
|---|------|------|------|
| 1 | hero CTA 点击 | `cta_click` `{label:"Try it",source_page:"/",destination:"/start"}` (AC1/AC2，委托器) | ✅ |
| 2 | demo tab 切换 | `demo_tab_switch` 仅真变化时发(trace→logs 1 次，重复点不发，→metric 第 2 次)，`tab∈{trace,logs,metric}` (AC4) | ✅ |
| 3 | 成本计算器拖动 | `cost_calculator_interact` 每挂载仅 1 次(连按 2 次仍 1)，`ingest_gb`/`retention_days` 为 number (AC2/AC4) | ✅ |
| 4 | 候补表单 API 200 | `waitlist_submit` 在 2xx 后触发，**props 为空**(AC3/AC5) | ✅ |
| 5 | 候补表单 API 429 | `waitlist_submit` **不触发**(AC3 反例) | ✅ |
| 6 | 候补表单 蜜罐 | 填蜜罐→zod `website:max(0)` 拦截 submit，**无 API 调用、不触发** track(AC3 反例) | ✅ |
| 7 | 设计伙伴 API 200 | `design_partner_submit` 在 2xx 后触发，props 为空(AC3/AC5) | ✅ |
| 8 | 设计伙伴 API 500 | `design_partner_submit` **不触发**(AC3 反例) | ✅ |
| 9 | 主题切换 | `theme_switch` `{theme:"dark"}` (AC2) | ✅ |
| 10 | 语言切换 | `locale_switch` `{from:"en",to:"zh",page:"/"}` (AC2) | ✅ |
| 11 | quickstart 复制 | `quickstart_copy` `{tab:"docker",snippet_type:"install"}`，复制成功后才发(AC2) | ✅ |
| 12 | 对比表展开 | `compare_table_expand` `{source_page:"/"}` (AC1/AC2) | ✅ |
| 13 | GitHub star 链接 | `github_star_click` `{source_page:"/design-partner"}` (AC2) | ✅ |
| 14 | changelog RSS | `rss_subscribe` 无 props(AC1) | ✅ |

- **AC6（无回归 / 构建）**：`pnpm check`（typecheck 0 err + lint 0 err + a11y 对比 26/26 + i18n parity 461=461）`exit 0`；`pnpm build` ✓ Compiled successfully。各组件原交互（tab/滑块/复制 toast/表单成功态/主题语言切换）E2E 中均正常。
- **AC7（命名文档化）**：11 事件名与 06§4.4 逐条一致；`lib/analytics.ts` docstring 已由 kebab 改 snake，无偏差。
- **AC5（无 PII）**：两表单事件 props 实测为 `null`；其余事件 props 仅含枚举/路径/标签，无邮箱姓名。

#### 4. 缺陷清单
- **本工单无阻断级或功能级缺陷。** 全部 11 点位按 §4.4 真实触发、props 与时序契约满足。
- 沿用 code-reviewer 已记发现 #1（委托器 `source_page` 用未去 locale 的 `location.pathname`，与 `locale_switch.page` 的去 locale 口径不一致，ZH 下 `source_page` 带 `/zh` 前缀 → 漏斗可能按语言分裂）：**数据质量观察项，非本期 AC 范围，不阻断 DONE**；建议 data-analyst 在补域名前统一口径。E2E 已实测 EN 下 `source_page` 为 `"/"`、`locale_switch.page` 为 `"/"`，本期单语口径自洽。
- 旁证（非缺陷）：蜜罐分支因 zod `website:max(0)` 在 RHF 解析层即被拦，onSubmit 不执行——其 silent-success UI 分支实际不可达；无论走哪条路径，结果都是「不打点、不调 API」，AC3 满足。可作为 code-review 备查的轻微冗余，不影响本期。

#### 5. 回归套件位置与重跑命令
- 配置：`playwright.config.ts`（root）；用例：`tests/e2e/analytics-funnel.spec.ts`（14 用例）。
- 重跑：`pnpm build && pnpm exec playwright test`（PATH 需含 node；见上）。新增 dev 依赖 `@playwright/test`。

#### 6. 放行建议
**建议放行（VERDICT PASS）**：AC1–AC7 全绿，11 事件真实浏览器触发 + props + 表单 2xx/4xx/5xx/蜜罐时序 + 去重 + 无 PII 均实测通过，构建无回归。AC8（真实 Plausible network 上报）按本期判过政策延后至补域名时复验，不阻断 DONE。data-analyst 的 `source_page` 口径统一建议宜在补域名前处理。

---

## 自动化测试 — ISSUE-5 [T08] UI token 迁移（globals.css + Geist Mono）

> 由 qa-automation 执行，2026-06-02。验证手段：静态门禁（pnpm check/build/a11y）+ 真实 Chromium E2E（Playwright，明暗双主题）。本工单为**纯前端设计 token + 字体**改造，无外部密钥/无 DB 依赖，全程本地验证。

### 1. 测试范围
- **AC1 字体**：`@fontsource/geist-mono` 依赖与 import；`--font-mono` 首位 Geist Mono；真实 `.font-mono` 元素 computed `font-family`；`document.fonts.check('16px "Geist Mono"')` 实际加载。
- **AC2 新 token**：`--brand/--amber/--warn`、radius 降档（sm=3px / xl=12px）、mono-text-scale、display-sm。
- **AC3 旧消费者整体变色**：legacy `--indigo/--marketing-accent` 重指向新色；`--marketing-hero-bg=none`；真实 `text-marketing-accent` 元素 computed 颜色为 Teal/Amber。
- **AC4 别名保留**：`@theme inline` 旧 `--color-indigo*/--color-marketing-accent*/--shadow-glow-*` 桥接仍在（代码核 + 0 元素掉样式）。
- **AC5 对比度 AA**：`pnpm a11y:contrast` 明暗双主题。
- **AC6 构建/类型/lint**：`pnpm check`、`pnpm build`。
- **AC7 双主题视觉 + 主题切换**：真实浏览器明暗双主题首页全屏截图人工目测 + 下拉切换 + 次级页面渲染 + 控制台 0 错误。
- **AC8 隔离性**：git diff 限定文件。

### 2. 执行环境与启动方式
- Node v23.6.1（nvm）/ pnpm 11.5.0 / Next.js 16.2.6 / Playwright 1.60.0（chromium）。
- 被测：生产构建 `pnpm build && PORT=<free> pnpm start`（端口 3100 被本机 kubectl port-forward 占用，改用 3217）。locale 路由 `as-needed`：EN=`/`、ZH=`/zh`。
- 静态门禁：`pnpm a11y:contrast` / `pnpm check` / `pnpm build`。
- E2E 重跑：`BASE=http://127.0.0.1:<port> node tests/e2e/issue5-ui-tokens.mjs`（服务需先起）。

### 3. 结果汇总（全部通过）

| 门禁/套件 | 结果 | 证据 |
|---|---|---|
| `pnpm a11y:contrast`（AC5） | **PASS 26/26** | 明暗双主题全部 ≥WCAG AA；light teal `#0f766e` 5.20/4.94、amber `#b45309` 4.77/4.53；dark 按钮文字 `#0e1117` on teal/amber 10.15/11.32:1 |
| `pnpm check`（AC6：typecheck+lint+a11y+i18n） | **PASS exit 0** | i18n EN↔ZH 504/504 parity OK |
| `pnpm build`（AC6） | **PASS exit 0** | 全站静态/SSG 路由构建成功，无回归 |
| E2E `issue5-ui-tokens.mjs`（AC1/2/3/4/7） | **PASS 29/29** | 见下方用例清单 |
| AC8 隔离性 | **PASS** | `git diff --name-only main...HEAD` 仅 `app/globals.css`/`app/layout.tsx`/`package.json`/`pnpm-lock.yaml`；`components/`、`messages/`、`app/api/`、`i18n/`、`lib/`、`scripts/` 零变更 |

**E2E 用例清单（29 项，明暗各一轮 + 切换 + 次级页）**：
- AC1：`--font-mono` 首位 Geist Mono ✓（明/暗）；Geist Mono webfont 实际加载 `document.fonts.check=true` ✓（明/暗）；真实 mono 元素 computed `font-family="Geist Mono", ui-monospace…` ✓（明/暗，样本含命令/代码 `abc123`）。
- AC2：`--radius-sm-token=3px` ✓、`--radius-xl-token=12px` ✓（明/暗）。
- AC3：`--brand`=light `rgb(15,118,110)`#0f766e / dark `rgb(45,212,191)`#2dd4bf ✓；`--amber`=light `rgb(180,83,9)`#b45309 / dark `rgb(251,191,36)`#fbbf24 ✓；legacy `--indigo`→brand teal ✓、`--marketing-accent`→amber ✓；`--marketing-hero-bg=none`（glow 移除）✓；**真实 `text-marketing-accent` 元素（16 处）computed color 实测为 Amber**（light rgb(180,83,9) / dark rgb(251,191,36)）✓。
- AC7：下拉主题切换 light→dark 实测 `data-theme` 由 light 变 dark ✓；ZH `/zh`（bodyText 1799）、QuickStart `/start`（1603）、Why `/why`（2223）均渲染内容 ✓；控制台/页面 0 未捕获错误 ✓。

**AC7 人工目测（双主题全屏截图）**：
- 截图：`test-results/issue5/home-light.png`、`home-dark.png`、`zh-home.png`。
- **Light**：Teal 强调文本/CTA、Amber 徽章（PRE 1.X / DESIGN PARTNER）、对比表 Teal/Amber 勾选、架构管线卡片圆角利落、QuickStart 代码块等宽呈现、Hero 无 radial glow。
- **Dark**：暖中性深底，亮 Teal 强调、Teal CTA 按钮**深色文字清晰可读**（`--primary-fg:#0e1117` 覆盖生效，验证 ui-designer/审查修复有效）、Amber 徽章/图表条/"Be early"卡片 Amber 边框，无掉色、无丢边框、切换无闪烁回归。
- 结论：明暗双主题关键区块（Hero / CrossSignalDemo / vs-表 / One-storage 管线 / QuickStart / DP CTA / 导航 / 页脚）均"利落+克制"，无视觉崩坏。

### 4. 缺陷清单
**无**。本轮 0 缺陷（含 0 阻断 / 0 严重 / 0 一般）。代码审查阶段已直接修复的 dark `--amber-fg` 漏覆盖（commit 487bbba）经本轮 a11y 26/26 + 暗色按钮可读性目测复核，确认闭合。

### 5. 回归套件位置与重跑命令
- 新增 E2E：`tests/e2e/issue5-ui-tokens.mjs`（29 断言，明暗双主题，可一键重跑，幂等）。
- 重跑（PATH 需含 node v23.6.1）：
  ```bash
  export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
  pnpm build && PORT=3217 pnpm start &   # locale: EN=/ ZH=/zh；若 3217 被占换端口
  BASE=http://127.0.0.1:3217 node tests/e2e/issue5-ui-tokens.mjs
  ```
- 静态门禁回归：`pnpm a11y:contrast && pnpm check && pnpm build`。

### 6. 放行建议
**建议放行（VERDICT: PASS）**：AC1–AC8 全部实测通过——a11y 26/26、check/build exit 0、E2E 29/29、双主题截图目测无崩坏、隔离性 git 可核。Geist Mono 真实加载并应用于 mono 元素，83/94 处旧工具类经值重指向整体切换 Teal/Amber 零掉色，明暗按钮前景可读。无阻断缺陷，建议合并并放行 T09/T10/T08b 后续组件改造。
