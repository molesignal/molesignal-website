# 08 — 测试报告（molesignal-website）

> 本文件由质量环写入。qa-tester 负责测试策略/用例；qa-automation 负责自动化执行与运行结果（下方"自动化测试"小节）。

---

## 自动化测试

### ISSUE-9 [T02] Design Partner 申请持久化 — 2026-06-02

**结论：VERDICT PASS**（本期缺密钥政策下 AC1–AC7 全部经真实执行验证通过；AC8 真实 Resend 写入联调延后补密钥复验，不阻塞 DONE）。阻断级缺陷 0。

#### 1. 测试范围
- **单测/脚本（mock fetch + 伪 env）**：`pnpm test:design-partner`（`scripts/check-design-partner-route.ts`）—— splitName 姓名拆分 5 例（AC2）、route zod 400/200（AC1）、真入库 POST 契约 + 姓名拆分 + 通知（AC2）、PII 最小化 body keys（AC5①）、缺 audience/缺 key 降级（AC3/AC4）、通知正文全 5 字段+replyTo+IP/时间戳（AC4）、蜜罐零副作用（AC5③）、密钥不入日志/截断（AC5④）。
- **接口（真服务 HTTP）**：`next start` + curl 实测 `POST /api/design-partner` —— 合法 200 / 缺字段·非法 email·非枚举·name>120·pain>400·坏 JSON·蜜罐填充 → 400；缺 RESEND env 时降级仍 200 + warn 日志；伪 env 探针证明配齐 key 时确实对外发出 contacts POST。
- **回归**：`pnpm test:audiences`（provider 不回归）、T11 限流（同 IP 第 6 次 → 429）。
- **E2E（真实 Chromium 浏览器）**：`tests/e2e/analytics-funnel.spec.ts` —— design_partner_submit 2xx 后 fire（无 PII props，AC6）、500 不 fire（AC6）、成功态 `role="status"` 原位持久卡可见（AC7）。
- **闸门**：`pnpm check`（typecheck+lint+a11y+i18n parity）+ `pnpm build`。
- **未覆盖**：AC8 真实 Resend partner audience 写入/导出（本期无密钥，延后复验）。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0 / Next.js 16.2.6（PATH 需含 `~/.nvm/versions/node/v23.6.1/bin`）。
- 接口测试：`PORT=<free> pnpm start`（生产构建）；注意本机 127.0.0.1:3100 被一 kubectl port-forward(nginx) 占用，改用空闲端口（实测 3287）。不配 `RESEND_*` env 即走本期默认降级路径；伪 env 探针单独以 `RESEND_API_KEY`/`RESEND_PARTNER_AUDIENCE_ID` 假值重启验证外呼。
- E2E：`pnpm exec playwright test`（webServer 自起 `next start -p 3210`，stub `window.plausible` 记录 track 调用）。

#### 3. 结果汇总（通过 8 类 / 失败 0）

| AC | 验证点 | 手段 | 运行结果 |
|----|--------|------|----------|
| AC1 | 5 字段 zod 校验 400/200 | 真服务 curl + 单测 | ✅ 合法 200；缺字段/非法 email/非枚举/name>120/pain>400/坏 JSON 全 400 |
| AC2 | 真入库接线 + 姓名拆分 | 单测 stubFetch（精确 body）+ live 伪 env 探针 | ✅ POST `/audiences/{id}/contacts`，`first_name=Ada`/`last_name=Lovelace`；单词名仅 first；live 探针证明确实外呼（真实 Resend 返 400 "API key invalid"），同时发通知邮件 |
| AC3 | 缺 audience id 降级 | 单测 | ✅ 跳过入库、仍发通知、仍 200、warn "audience id missing — skipping list insert" |
| AC4 | 缺 API key 降级 + 通知 5 字段 | 单测 + live 日志 | ✅ 入库+通知双 skip+warn、仍 200、不抛错；通知正文含 Name/Email/Company size/Current stack/Biggest pain + replyTo=申请人 + IP/时间戳尾注 |
| AC5 | PII/安全 | 单测 + live | ✅ 入库 body keys = `email,first_name,last_name,unsubscribed`（无 size/stack/pain/IP）；audienceId 仅来自 env；蜜罐填充→zod 400 零入库/零通知；密钥不入日志（截断验证） |
| AC6 | 埋点时序不回归 | E2E 真浏览器 | ✅ design_partner_submit 仅 2xx 后 fire（props=null）、500 不 fire |
| AC7 | 成功态原位持久卡 + 闸门 | E2E + check + build | ✅ `getByRole("status")` 可见；`pnpm check` 0 报错（a11y 26/26 AA、i18n 506↔506 parity）；`pnpm build` exit 0 |
| AC8 | 真实 Resend 联调 | — | ⏸ 延后（本期缺密钥政策，不阻塞 DONE） |

**执行明细**：
- `pnpm test:design-partner`：全部断言通过（splitName 5 + AC1 8 + AC2 + AC5① + AC3 + AC4×2 + AC5③×2 + AC5④），exit 0。
- `pnpm test:audiences`（回归）：全过，exit 0。
- E2E `analytics-funnel.spec.ts`：14/14 passed（含 #7 design_partner_submit 2xx fire、#8 500 不 fire）。
- T11 限流回归：同 IP req#1–5 → 200，req#6 → 429。
- 真服务 curl：valid→200、6 类非法输入→400、honeypot→400、缺 RESEND env→200+warn、伪 env→200(fail-soft)+外呼日志。

#### 4. 缺陷清单
**阻断/严重/一般缺陷：0。** 无新发现缺陷。

已知非阻塞观察项（开发/审查环已记录，本环复核确认不影响放行）：
- **蜜罐 silent-200 分支为死代码**：`website: z.string().max(0)` 使填充值在 zod 层即被拒为 400（与 cloud-waitlist/ISSUE-8 同构）。安全保证（零入库/零通知/零 track）仍成立——经 400 而非 silent 200，前端因非 2xx 不触发 track。**实测确认，建议维持现状**（要真"silent 200"需改 schema，超本单范围）。
- **`lib/email.ts` 错误日志未截断上游 body**（pre-existing，本单不动 sendEmail）：现实中 Resend 错误响应不含调用方密钥，风险低；`addContact` 路径已截断（AC5④ 已验）。建议另开工单统一截断。

#### 5. 回归套件位置与重跑命令
```bash
export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
cd <产品目录>
pnpm test:design-partner          # 路由接线 + splitName + 降级 + 安全（scripts/check-design-partner-route.ts）
pnpm test:audiences               # provider 不回归
pnpm exec playwright test tests/e2e/analytics-funnel.spec.ts   # 埋点时序 + 成功态（AC6/AC7）
pnpm check && pnpm build          # 类型/lint/a11y/i18n + 生产构建闸门
```

#### 6. 放行建议
**建议放行（VERDICT: PASS）**。AC1–AC7 全部经真实执行（单测 + 起服务 curl + 真实浏览器 E2E）客观验证通过，零阻断缺陷；AC8 真实 Resend 写入联调按本期缺密钥政策延后补密钥时复验，不阻塞本期关单/DONE。

---

### ISSUE-1 [T11] 可靠限流后端（Upstash + 内存兜底）— 2026-06-02

**结论：VERDICT PASS**（本期 AC1–AC7 全过；AC8 真实跨实例延后，无密钥不验，不阻塞 DONE）。

#### 1. 测试范围
- **接口（HTTP E2E）**：`POST /api/cloud-waitlist`（10/h）、`POST /api/design-partner`（5/h）的限流行为——阈值、429 响应体/`Retry-After` 头、两表单 key 独立计数。
- **降级路径**：无 `UPSTASH_*` env → 内存兜底（本期默认生产路径）正常服务 + fallback 警告只打一次（AC2）。
- **容错路径**：配置 `UPSTASH_*` 但后端不可达 → fail-open 放行、不返回 5xx（AC6）。
- **安全（附加）**：H1 — 客户端伪造 `X-Forwarded-For` 最左段不能轮换绕过限流。
- **未覆盖**：AC8 真实 Upstash 跨实例共享计数（本期无密钥，延后复验）；AC7 构建门禁属 CI 域，由开发/审查环跑（本环按"运行时验证"口径不复跑）。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0 / Next.js 16.2.6（Turbopack）。
- 启动：`PORT=3199 pnpm dev -p 3199`（Ready 245ms）。`RESEND_API_KEY` 未配 → 邮件发送被跳过（`{skipped:true}`），有效提交无外部副作用。
- 驱动：`curl` 真实 HTTP 请求，捕获状态码 + `Retry-After` 头 + 服务端日志。
- fail-open 子场景：以 `UPSTASH_REDIS_REST_URL=https://127.0.0.1:1`、`UPSTASH_REDIS_REST_TOKEN=bogus-token` 重启实例。

#### 3. 结果汇总

| AC | 验证点 | 运行结果 | 证据 |
|----|--------|----------|------|
| AC1 | async 接线（await 生效） | ✅ 超阈值由 200 切 429，证明 await rateLimit 生效 | 见 AC3 |
| AC3 | 阈值不变、key 独立 | ✅ dp 5×200 后 429；cloud 10×200 后 429；两表单互不影响 | 下方日志 |
| AC4 | 429 + Retry-After 非负整数 | ✅ `Retry-After: 3600`，响应体结构未变 | 下方日志 |
| AC5 | 可复现 (max+1)→429 | ✅ 两表单各稳定复现 | 下方日志 |
| AC2 | 缺 env 降级 + warn 一次 | ✅ ~26 请求仅 1 条 fallback 警告，不崩 | 日志 grep -c =1 |
| AC6 | fail-open 不返回 5xx | ✅ Upstash 不可达，8 次（超 max）全 200，每请求 1 条 failing-open 警告 | 下方日志 |
| H1 | XFF 最左段不可绕过 | ✅ 轮换最左段、固定尾段 → 第 6 次起 429 | 下方日志 |
| AC7 | 构建门禁 | （CI 域，开发/审查环已跑，本环不复跑） | — |
| AC8 | 真实跨实例 | 延后（无密钥） | — |

**用例清单与结果**（关键运行证据）：

```
# design-partner (max=5/h)
req#1..5 -> HTTP 200 {"ok":true}
req#6   -> HTTP 429  Retry-After=3600  {"error":"Too many requests. Try again later."}
req#7   -> HTTP 429  Retry-After=3600

# cloud-waitlist (max=10/h)  —— 独立计数（design-partner 已耗尽不影响）
req#1..10 -> HTTP 200
req#11/12 -> HTTP 429  Retry-After=3600

# AC2 内存兜底 warn-once
grep -c "falling back to" server.log = 1   （全程约 26 次请求）

# AC6 fail-open（bogus UPSTASH_*，host 不可达）
req#1..8 -> HTTP 200      （全部放行，超 max 5 仍不限、无 5xx）
grep -c "failing open" log = 8 ; grep -c "falling back to" log = 0
日志: [rate-limit] Upstash limit() failed — failing open (request allowed): TypeError: fetch failed

# H1 安全探针（轮换 XFF 最左段，固定尾段 7.7.7.7）
XFF "10.0.0.1, 7.7.7.7" .. "10.0.0.5, 7.7.7.7" -> 200 ×5
XFF "10.0.0.6, 7.7.7.7"                          -> 429   （最左段轮换无法绕过）
```

#### 4. 缺陷清单
无。阻断级缺陷 0。

#### 5. 回归套件位置与重跑命令
- **单元/脚本级**（内存兜底 + fail-open 子进程断言）：`pnpm test:rate-limit`（`scripts/check-rate-limit.ts`）。
- **HTTP 层手动驱动**（重跑步骤）：
  1. `PORT=3199 pnpm dev -p 3199`
  2. 对 `/api/design-partner` 连发 6 次有效 POST → 期望 5×200 后 429（`Retry-After` 非负整数）。
  3. 对 `/api/cloud-waitlist` 连发 11 次 → 期望 10×200 后 429。
  4. fail-open：带 bogus `UPSTASH_*` 重启，连发超阈值 → 期望全 200 + 日志 "failing open"。

#### 6. 放行建议
本期必过项 AC1–AC7 全部通过（运行时真实验证），无阻断缺陷。**建议合并放行**。AC8（真实 Upstash 跨实例）待补 `UPSTASH_*` 密钥后复验，按用户决策不阻塞本期 DONE。

---

### ISSUE-3 [T04] 死链/画饼治理 — 运行时验证（qa-automation, 2026-06-02）

被验证工单：`backlog/ISSUE-3.md`（P0·light 道）。分支 `feature/ISSUE-3`（commits 80668ae / 17eddcb / a8d9560）。

#### 1. 测试范围
- **链接回归（lint:links）**：全站 33 个 URL（EN+ZH 全页 + sitemap/robots/rss/og）真实 HTTP 抓取，断言 2xx/3xx。
- **i18n 双语 parity（lint:i18n）**：messages/en.json ↔ zh.json 键集合一致性。
- **HTML 渲染断言（curl）**：docs.molesignal.io 死链清除、无 `href="#"` 占位、`#install` 锚点目标存在、helm/binary v1.0 notice 双语渲染。
- **交互式 E2E（Playwright + Chromium，真实浏览器）**：Discord/Twitter 禁用态不可点、Resources 下拉无 Docs、Download → /start#install 真实跳转并滚动到安装区、helm/binary tab 点击后 v1 notice 可见而 docker tab 不显示。

#### 2. 执行环境与启动方式
- Node v23.6.1（`~/.nvm/versions/node/v23.6.1/bin` 需入 PATH）、pnpm 11.5.0。
- `pnpm dev`（Next.js 16.2.6 Turbopack，:3000）。**注意**：首跑撞到全站 500（`SyntaxError: Unexpected non-whitespace ... JSON`），根因是仓库内残留的 `next build` 产物 `.next/` 与 `next dev` Turbopack 布局冲突——属环境脏缓存，非 T04 代码缺陷；`rm -rf .next` 重启后全部 200。**建议**：`.gitignore` 已含 `.next`，但工作树中存在历史产物，devops/引擎在 dev 前应清缓存。
- E2E：`node tests/e2e/issue3-t04-deadlinks.mjs`（standalone Playwright，直驱已装 chromium，对 dev :3000）。

#### 3. 结果汇总
| 检查 | 结果 |
|---|---|
| `pnpm lint:links` | 33 URL，**32 ok / 1 FAIL**（`/opengraph-image` 404）→ 见缺陷①，**非 T04 引入**；T04 新增/改动的 4 条内链 `/roadmap`、`/changelog`、`/blog`、`/start#install` 全 200（EN+ZH） |
| `pnpm lint:i18n` | **PASS** — EN 466 / ZH 466 键，parity OK（AC5 ✓） |
| HTML 渲染断言 | docs.molesignal.io = 0、`href="#"` = 0、`#install` 目标存在、v1Notice EN+ZH 均渲染 — 全 ✓ |
| Playwright E2E（13 断言） | **13/13 PASS**（AC1/AC2/AC3/AC4 全覆盖） |

E2E 13 断言明细（全 PASS）：无 docs 锚点 / 无 href="#" / footer Discord 非锚点 / footer Discord aria-disabled / footer Twitter 非锚点 / community-callout Discord 卡禁用 / Resources 菜单含 Download / Resources 菜单无 Docs / Download 跳转 `/start#install`（实测 URL=`http://localhost:3000/start#install`）/ `#install` 锚点滚入视口（y=777）/ helm tab 显 v1 notice / binary tab 显 v1 notice / docker tab 不显 v1 notice。

AC 逐条对照：① Footer/TopNav 移除 docs ✓ | ② Discord/Twitter 禁用态非 href# ✓（footer+community 双处实测） | ③ Download coming-soon→`/start#install` 真实跳转+滚动 ✓ | ④ helm/binary v1.0 target notice ✓ | ⑤ messages 双语同步 466/466 ✓ | ⑥ lint:links 全 2xx：**T04 范围内链全绿**，仅 1 条预存且与 T04 无关的 og 路由 404（见缺陷①）。

#### 4. 缺陷清单
- **缺陷①〔非阻断·非本工单·建议另立工单〕** `/opengraph-image` 在 dev 下 404。
  - 复现：`pnpm dev` → `curl -i http://localhost:3000/opengraph-image` → 404。
  - 预期 vs 实际：lint:links seed 期望 2xx；实际 404。
  - 责任模块：`app/opengraph-image.tsx`（OG 元数据路由）。
  - **判定不阻断 T04**：① 该文件与 `main` 完全一致（`git diff main..HEAD` 为空），ISSUE-3 未触碰；② seed 早在 `main` 的 check-links.ts 即存在；③ 属预存问题（疑似 metadata 图像路由在 Turbopack dev 下不走该路径，需 `next build`/`next start` 验证）。建议 devops/T12 单独跟进 OG 路由产线验证，不应回流 ISSUE-3。
- **观察项〔非缺陷·脚本路径 bug〕** `pnpm lint:quickstart` 失败 4/9，全因脚本去 `workspace/README.md`（项目父目录）读取 README 而文件不在那；网站侧 5 项全 ok。与 T04 无关（T04 未改 quickstart 命令/README），且不在 ISSUE-3 AC 内。建议修 `scripts/check-quickstart-sync.ts` 的 ROOT 路径。

#### 5. 回归套件位置与重跑命令
- 链接/i18n：`pnpm lint:links`（需 `pnpm dev` 在 :3000）、`pnpm lint:i18n`。
- T04 交互 E2E：`node tests/e2e/issue3-t04-deadlinks.mjs`（需 `pnpm dev` 在 :3000；产出截图 `test-results/issue3-t04-binary-notice.png`）。
- 重跑前置：PATH 含 node v23.6.1；若全站 500 先 `rm -rf .next` 再 `pnpm dev`。

#### 6. 放行建议
**VERDICT: PASS** — ISSUE-3 全部 6 条 AC 实测通过（lint:i18n 绿、T04 范围内链全 2xx、13/13 浏览器 E2E 绿、HTML 断言全绿、截图佐证）。唯一 lint:links 失败项 `/opengraph-image` 经核为**预存且与 T04 无关**，不阻断本工单放行；另建议另立工单跟进 OG 路由与 lint:quickstart 脚本路径。

---

### ISSUE-4 [T06] 隐私/条款真实法务文本 — 运行时验证（qa-automation, 2026-06-02）

**结论：VERDICT PASS** — AC1–AC8 全部实测通过；唯一 lint:links 失败 `/opengraph-image` 404 为预存项（ISSUE-3/T04 已记录），非本页链接、与 T06 无关，不阻断放行。

#### 1. 测试范围
- **内容 E2E（浏览器，真 Chromium）**：`/privacy /terms /zh/privacy /zh/terms` 四页——六大隐私覆盖点文本、terms 独立真实条款、EN/ZH 双语无残英/无占位、日期、链接锚点、主题切换不崩。
- **内容断言（HTTP）**：curl 拉取四页渲染 HTML，逐条 grep 覆盖点 + 无 `stub` 残留。
- **链接门禁**：`lint:links` 全站爬（生产构建）；apache 外链独立 2xx 复核。
- **静态门禁**：`typecheck` / `lint:i18n`（parity）/ `a11y:contrast` / `check` / `build`。
- **回归**：全量 E2E 套件 20/20（14 既有 analytics-funnel + 6 新增 legal），确认本分支 `playwright.config.ts` 改动（reducedMotion 移入 contextOptions）未回归。
- **未覆盖/不适用**：无外部密钥即可全程验证（纯文案/i18n，不依赖 Resend/Plausible/Upstash env）。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0 / Next.js 16.2.6。PATH 需含 `~/.nvm/versions/node/v23.6.1/bin`。
- HTTP 断言：`PORT=3137 pnpm build && PORT=3137 pnpm start`（生产构建，SSG 预渲染四页）→ curl。
- E2E：`pnpm exec playwright test`（config 自带 webServer，`next start -p 3210` 生产构建）。
- 链接：`SITE=http://localhost:3137 pnpm lint:links`（须打生产构建；dev/Turbopack 并发爬会进 JSON.parse 500 坏态，见“观察项”）。

#### 3. 结果汇总

| AC | 验证点 | 运行结果 | 证据 |
|----|--------|----------|------|
| AC1 | 隐私页六覆盖点（字段/Plausible无cookie不存IP不跨站DNT/Resend/IP限流/localStorage非追踪/founders联系+不出售） | ✅ EN 16 项 grep 全 PASS + E2E 断言全绿；无 stub | HTML 断言 + E2E#1 |
| AC2 | 条款页独立真实（as-is/Apache-2.0/可接受使用/无担保/可更新） | ✅ 与 privacy 彻底分开、stub 已删 | HTML 断言 + E2E#3 |
| AC3 | EN/ZH parity + ZH 对等翻译非占位 | ✅ EN/ZH 各 504 键 parity OK；legal 数组同构（privacy 8/8、terms 5/5）；ZH 无残英 | `lint:i18n` 绿 + E2E#4/#5 |
| AC4 | 两页日期=2026-06-02 | ✅ 四页均含 `2026-06-02` | HTML 断言 + E2E |
| AC5 | 文本忠于事实（F1 Resend仅传输不入名单 / F2 IP+时间戳+字段作通知邮件留存 / F3 服务器日志 / 无虚假cookie/出售声明） | ✅ 文案与 security-auditor F1–F4 口径一致，逐条在页面文本核到 | messages dump + HTML 断言 |
| AC6 | 页内链接 2xx（mailto/apache/站内） | ✅ mailto+apache 为真实 `<a href>`；apache 外链 200；四法务页 + /cloud /design-partner 全 2xx | `lint:links`（生产）+ E2E 锚点断言 |
| AC7 | check+build 通过、四态渲染无崩 | ✅ `pnpm check` exit 0、`pnpm build` exit 0、E2E 主题切换无 pageerror | check/build 日志 + E2E#6 |
| AC8 | 免责声明就位 | ✅ disclaimer「plain-language summary, not formal legal advice…email founders@」渲染于两页 | messages dump + HTML |

#### 4. 用例清单与结果
- HTML 内容断言：EN privacy 16/16 PASS、EN terms 8/8、ZH privacy 7/7、ZH terms（担保/Apache/日期）PASS、四页均无 `placeholder for v1 launch`。
- 链接锚点：四页 `mailto:founders@molesignal.io` 真实锚点；两 terms 页 `https://www.apache.org/licenses/LICENSE-2.0` 真实锚点；无裸 URL 文本。
- E2E：6/6 新增 legal 用例绿；全量 20/20 绿。
- 门禁：typecheck exit 0、lint:i18n 504/504 OK、a11y:contrast 26/26 AA、check exit 0、build exit 0。
- lint:links（生产构建）：32/33 ok，唯一 FAIL=`/opengraph-image 404`（预存、非本页链接）。

#### 5. 缺陷清单
- **无阻断级缺陷。**
- 〔观察项·非阻断·dev-only〕`pnpm dev`（Turbopack）下被 `lint:links` 并发爬时，全站路由进入 `SyntaxError: Unexpected non-whitespace character after JSON`（proxy/middleware）坏态返 500，且不自愈。**生产构建（`build`+`start`）下全程 200、E2E 全绿，不可复现**——判为 dev/Turbopack HMR 并发坏态，与本工单内容无关，不影响上线。建议另立工单排查 dev 体验（修复前：dev 出现全站 500 时 `rm -rf .next` 重启；链接门禁一律打生产构建跑）。
- 〔预存·非本工单〕`/opengraph-image` 404：ISSUE-3/T04 已记录，留对应工单处理。

#### 6. 回归套件位置与重跑命令
- 新增 E2E：`tests/e2e/issue4-legal.spec.ts`（6 用例，含 EN/ZH×privacy/terms 覆盖点、链接锚点、主题切换韧性）。
- 重跑：`export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"`
  - 内容+回归：`pnpm exec playwright test`（或仅本工单 `… playwright test tests/e2e/issue4-legal.spec.ts`）
  - 门禁：`pnpm check && pnpm build`
  - 链接（须生产构建）：`PORT=3137 pnpm start &` → `SITE=http://localhost:3137 pnpm lint:links`

#### 7. 放行建议
**VERDICT: PASS** — ISSUE-4 八条 AC 全部实测通过（内容六覆盖点齐、双语 parity 504/504、日期准、AC5 文案与 security-auditor F1–F4 纠偏一致、链接锚点 2xx、check+build 绿、20/20 E2E 绿）。建议合并回 main。dev-only 全站 500 观察项与 `/opengraph-image` 404 均为既有/环境项，不阻断本 P0 内容工单。

---

## 代码审查

### ISSUE-2 [T05] 转化漏斗埋点接线（11 点位）— code-reviewer, 2026-06-02

**审查范围**：`git diff main...feature/ISSUE-2`（24 文件，+379/-12），含 `components/analytics/analytics-clicks.tsx`（新增委托式点击追踪器）、7 个客户端组件直调 `track()`、11 处服务端组件 `data-analytics-*` 标注、`lib/analytics.ts` docstring 修正。

**结论：0 必改，无阻断缺陷。建议放行到 QA 自动化验证。**

#### 审查口径与验证
- **构建/静态门禁**（本地实跑，Node v23.6.1）：`pnpm typecheck` ✅ 0 error；`pnpm lint` ✅ 0 error / 5 warning（5 条均为 `theme-toggle.tsx:37` 与 `toc.tsx:48` 的既有 `set-state-in-effect`，与本次埋点改动无关——本次 theme-toggle 改动在 `:78`）。与开发记录自测一致。
- **AC1 全覆盖 + 无多余**：11 事件名全部接线，命名全为 `snake_case` 动宾、与 06§4.4 逐条一致。证据：`track("…")` 直调 7 个事件（`demo_tab_switch`/`cost_calculator_interact`/`quickstart_copy`/`waitlist_submit`/`design_partner_submit`/`locale_switch`/`theme_switch`）+ `data-analytics-event="…"` 标注 4 个事件（`cta_click`/`compare_table_expand`/`github_star_click`/`rss_subscribe`）。无 kebab、无 §4.4 之外的事件。
- **AC3 表单时序（核心风险点）**：✅ 正确。`cloud-waitlist-form.tsx:64` / `design-partner-form.tsx:66` 均在 `if (!res.ok) throw` 之后、`setSubmitted(true)` 同分支调用，且无 props。zod 失败被 react-hook-form 拦在 onSubmit 外、429/5xx/网络落 catch、蜜罐分支在 fetch 前 `return` 且无 `track()`（遵架构裁决，bot 不污染漏斗）。三条反例均不触发。
- **AC4 去重**：✅ `cost-calculator.tsx` `useRef(false)` 守卫，首次任一滑块拖动发一次；`cross-signal-demo.tsx:53` 仅 `key !== active` 时发，且自动巡演走 `setActive`（:36-44）不经 `pick`，故巡演不误发——已核对源码确认。
- **AC5 无 PII**：✅ 两表单事件无 props；`locale_switch.page` 用 i18n 的 locale-strip pathname；`cta_click.destination` 为静态路由/`mailto:founders`（非用户数据）。无邮箱/姓名/IP。
- **委托追踪器正确性**（`analytics-clicks.tsx`）：✅ capture 阶段监听早于 Next `<Link>` 导航，事件不丢；`closest("[data-analytics-event]")` 正确处理图标/`<span>` 子元素点击；`JSON.parse` 包 try/catch 容错；`track()` 缺域名安全 no-op。单一 document 监听挂在 `layout.tsx`，跨路由不重复挂载。**无双发风险**：`ThemeToggle`/`LocaleSwitcher`/demo 按钮/表单提交均无 `data-analytics-event` 且不嵌套于带该属性的祖先，委托器对其返回 null。
- **契约**：✅ 不动 API 出入参、不动数据模型、不加依赖。唯一组件签名变更是 `CodeBlock`/`CodeBlockCopy` 新增可选 `analytics` prop（客户端内部，非对外契约），向后兼容（未传则不发）。与架构评估一致。
- **提交规范**：✅ `feat(ISSUE-2): 转化漏斗埋点接线(11点位)`，符合 Conventional Commits。

#### 发现清单

| # | 位置 | 类型 | 级别 | 说明 / 建议 |
|---|------|------|------|------|
| 1 | `analytics-clicks.tsx:30` vs `locale-switcher.tsx:39` | 坏味道（数据一致性） | 建议改 | `source_page`/`page` 维度口径不一致：委托器用 `window.location.pathname`（`localePrefix:"as-needed"` → EN `/why`、ZH `/zh/why`），而 `locale_switch.page` 用 i18n `usePathname()`（locale-strip → 两语均 `/why`）。结果 `cta_click` 等的 `source_page` 会**按语言分裂漏斗**，与 `locale_switch.page` 对不上。建议委托器也用 locale-strip 的 path（或反之统一），具体取舍交 data-analyst 定。非功能 bug，不阻断本期。 |
| 2 | `theme-toggle.tsx:78` | 设计权衡 | 可选 | `theme_switch.theme` 仅发 `light`/`dark`（toggle 不暴露 `system`），§4.4 枚举含 `system` 但永不触发。data-analyst 已确认为预期（UI 无 system 选项），记录备查。 |
| 3 | CTA 边界（开发记录已列） | 风格/判断 | 可选 | 内联文字链、start 页 next-steps 卡片、404/500 恢复按钮刻意不计 `cta_click`。口径一致且可判定；若 QA/data-analyst 认为某内联链应计入，补 `data-analytics-event` 即可（零结构改动）。 |

#### 放行建议
正确性、契约、命名、时序、去重、PII 六项均达标，typecheck/lint/build 三关本地实跑通过，无必改项。**建议放行到 qa-automation 做运行时验证**（spy `window.plausible`：11 事件触发、props、表单 2xx/4xx/蜜罐三路径、去重）。发现 #1（source_page 语言分裂）建议在本期或紧邻迭代由 data-analyst 拍板统一口径——不阻断 DONE，但会影响漏斗数据质量，宜尽早。

---

### ISSUE-2 [T05] 转化漏斗埋点接线(11点位) — 运行时 E2E 验证（qa-automation, 2026-06-02）

**结论：VERDICT PASS**（AC1–AC7 全绿；AC8 真实 Plausible 上报本期无域名延后，不阻断 DONE）。

#### 1. 测试范围
- **真实浏览器 E2E**（Playwright + Chromium，对**生产构建** `next start` 实跑）：在每次导航前注入 `window.plausible` 录制桩（事件落 `sessionStorage`，跨同标签页客户端导航存活），逐一触发 11 个交互并断言 **事件名 + props + 触发时序**。
- 覆盖 §4.4 全部 11 事件；重点压 **AC3 表单时序三路径**（2xx 触发 / 4xx·5xx 不触发 / 蜜罐不触发）与 **AC4 去重**（demo 同 tab 重复点、计算器每挂载一次）。
- **静态全量核对**：8 处 `track()` 直调 + 27 处 `data-analytics-event` 标注，11 事件名逐条比对 06§4.4，全 `snake_case`、无遗漏无多余。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0；`pnpm build` 产物，Playwright `webServer` 自动起 `next start -p 3210`（`reuseExistingServer`），跑完自动回收（已确认端口 3210 无残留进程）。
- 未配 `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` → `PlausibleScript` 返回 null（不注入真实脚本），录制桩存活，符合本期判过政策（spy/mock 客观验证即判 `[x]`）。
- 重跑命令：
  ```bash
  export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
  cd <repo>
  pnpm build && pnpm exec playwright test          # 回归套件（14 用例）
  ```
  套件位置：`playwright.config.ts` + `tests/e2e/analytics-funnel.spec.ts`。

#### 3. 结果汇总：14/14 通过，0 失败

| # | 用例 | 事件 / 验收点 | 结果 |
|---|------|------|------|
| 1 | hero CTA 点击 | `cta_click` `{label:"Try it",source_page:"/",destination:"/start"}` (AC1/AC2，委托器) | ✅ |
| 2 | demo tab 切换 | `demo_tab_switch` 仅真变化时发(trace→logs 1 次，重复点不发，→metric 第 2 次)，`tab∈{trace,logs,metric}` (AC4) | ✅ |
| 3 | 成本计算器拖动 | `cost_calculator_interact` 每挂载仅 1 次(连按 2 次仍 1)，`ingest_gb`/`retention_days` 为 number (AC2/AC4) | ✅ |
| 4 | 候补表单 API 200 | `waitlist_submit` 在 2xx 后触发，**props 为空**(AC3/AC5) | ✅ |
| 5 | 候补表单 API 429 | `waitlist_submit` **不触发**(AC3 反例) | ✅ |
| 6 | 候补表单 蜜罐 | 填蜜罐→zod `website:max(0)` 拦截 submit，**无 API 调用、不触发** track(AC3 反例) | ✅ |
| 7 | 设计伙伴 API 200 | `design_partner_submit` 在 2xx 后触发，props 为空(AC3/AC5) | ✅ |
| 8 | 设计伙伴 API 500 | `design_partner_submit` **不触发**(AC3 反例) | ✅ |
| 9 | 主题切换 | `theme_switch` `{theme:"dark"}` (AC2) | ✅ |
| 10 | 语言切换 | `locale_switch` `{from:"en",to:"zh",page:"/"}` (AC2) | ✅ |
| 11 | quickstart 复制 | `quickstart_copy` `{tab:"docker",snippet_type:"install"}`，复制成功后才发(AC2) | ✅ |
| 12 | 对比表展开 | `compare_table_expand` `{source_page:"/"}` (AC1/AC2) | ✅ |
| 13 | GitHub star 链接 | `github_star_click` `{source_page:"/design-partner"}` (AC2) | ✅ |
| 14 | changelog RSS | `rss_subscribe` 无 props(AC1) | ✅ |

- **AC6（无回归 / 构建）**：`pnpm check`（typecheck 0 err + lint 0 err + a11y 对比 26/26 + i18n parity 461=461）`exit 0`；`pnpm build` ✓ Compiled successfully。各组件原交互（tab/滑块/复制 toast/表单成功态/主题语言切换）E2E 中均正常。
- **AC7（命名文档化）**：11 事件名与 06§4.4 逐条一致；`lib/analytics.ts` docstring 已由 kebab 改 snake，无偏差。
- **AC5（无 PII）**：两表单事件 props 实测为 `null`；其余事件 props 仅含枚举/路径/标签，无邮箱姓名。

#### 4. 缺陷清单
- **本工单无阻断级或功能级缺陷。** 全部 11 点位按 §4.4 真实触发、props 与时序契约满足。
- 沿用 code-reviewer 已记发现 #1（委托器 `source_page` 用未去 locale 的 `location.pathname`，与 `locale_switch.page` 的去 locale 口径不一致，ZH 下 `source_page` 带 `/zh` 前缀 → 漏斗可能按语言分裂）：**数据质量观察项，非本期 AC 范围，不阻断 DONE**；建议 data-analyst 在补域名前统一口径。E2E 已实测 EN 下 `source_page` 为 `"/"`、`locale_switch.page` 为 `"/"`，本期单语口径自洽。
- 旁证（非缺陷）：蜜罐分支因 zod `website:max(0)` 在 RHF 解析层即被拦，onSubmit 不执行——其 silent-success UI 分支实际不可达；无论走哪条路径，结果都是「不打点、不调 API」，AC3 满足。可作为 code-review 备查的轻微冗余，不影响本期。

#### 5. 回归套件位置与重跑命令
- 配置：`playwright.config.ts`（root）；用例：`tests/e2e/analytics-funnel.spec.ts`（14 用例）。
- 重跑：`pnpm build && pnpm exec playwright test`（PATH 需含 node；见上）。新增 dev 依赖 `@playwright/test`。

#### 6. 放行建议
**建议放行（VERDICT PASS）**：AC1–AC7 全绿，11 事件真实浏览器触发 + props + 表单 2xx/4xx/5xx/蜜罐时序 + 去重 + 无 PII 均实测通过，构建无回归。AC8（真实 Plausible network 上报）按本期判过政策延后至补域名时复验，不阻断 DONE。data-analyst 的 `source_page` 口径统一建议宜在补域名前处理。

---

## 自动化测试 — ISSUE-5 [T08] UI token 迁移（globals.css + Geist Mono）

> 由 qa-automation 执行，2026-06-02。验证手段：静态门禁（pnpm check/build/a11y）+ 真实 Chromium E2E（Playwright，明暗双主题）。本工单为**纯前端设计 token + 字体**改造，无外部密钥/无 DB 依赖，全程本地验证。

### 1. 测试范围
- **AC1 字体**：`@fontsource/geist-mono` 依赖与 import；`--font-mono` 首位 Geist Mono；真实 `.font-mono` 元素 computed `font-family`；`document.fonts.check('16px "Geist Mono"')` 实际加载。
- **AC2 新 token**：`--brand/--amber/--warn`、radius 降档（sm=3px / xl=12px）、mono-text-scale、display-sm。
- **AC3 旧消费者整体变色**：legacy `--indigo/--marketing-accent` 重指向新色；`--marketing-hero-bg=none`；真实 `text-marketing-accent` 元素 computed 颜色为 Teal/Amber。
- **AC4 别名保留**：`@theme inline` 旧 `--color-indigo*/--color-marketing-accent*/--shadow-glow-*` 桥接仍在（代码核 + 0 元素掉样式）。
- **AC5 对比度 AA**：`pnpm a11y:contrast` 明暗双主题。
- **AC6 构建/类型/lint**：`pnpm check`、`pnpm build`。
- **AC7 双主题视觉 + 主题切换**：真实浏览器明暗双主题首页全屏截图人工目测 + 下拉切换 + 次级页面渲染 + 控制台 0 错误。
- **AC8 隔离性**：git diff 限定文件。

### 2. 执行环境与启动方式
- Node v23.6.1（nvm）/ pnpm 11.5.0 / Next.js 16.2.6 / Playwright 1.60.0（chromium）。
- 被测：生产构建 `pnpm build && PORT=<free> pnpm start`（端口 3100 被本机 kubectl port-forward 占用，改用 3217）。locale 路由 `as-needed`：EN=`/`、ZH=`/zh`。
- 静态门禁：`pnpm a11y:contrast` / `pnpm check` / `pnpm build`。
- E2E 重跑：`BASE=http://127.0.0.1:<port> node tests/e2e/issue5-ui-tokens.mjs`（服务需先起）。

### 3. 结果汇总（全部通过）

| 门禁/套件 | 结果 | 证据 |
|---|---|---|
| `pnpm a11y:contrast`（AC5） | **PASS 26/26** | 明暗双主题全部 ≥WCAG AA；light teal `#0f766e` 5.20/4.94、amber `#b45309` 4.77/4.53；dark 按钮文字 `#0e1117` on teal/amber 10.15/11.32:1 |
| `pnpm check`（AC6：typecheck+lint+a11y+i18n） | **PASS exit 0** | i18n EN↔ZH 504/504 parity OK |
| `pnpm build`（AC6） | **PASS exit 0** | 全站静态/SSG 路由构建成功，无回归 |
| E2E `issue5-ui-tokens.mjs`（AC1/2/3/4/7） | **PASS 29/29** | 见下方用例清单 |
| AC8 隔离性 | **PASS** | `git diff --name-only main...HEAD` 仅 `app/globals.css`/`app/layout.tsx`/`package.json`/`pnpm-lock.yaml`；`components/`、`messages/`、`app/api/`、`i18n/`、`lib/`、`scripts/` 零变更 |

**E2E 用例清单（29 项，明暗各一轮 + 切换 + 次级页）**：
- AC1：`--font-mono` 首位 Geist Mono ✓（明/暗）；Geist Mono webfont 实际加载 `document.fonts.check=true` ✓（明/暗）；真实 mono 元素 computed `font-family="Geist Mono", ui-monospace…` ✓（明/暗，样本含命令/代码 `abc123`）。
- AC2：`--radius-sm-token=3px` ✓、`--radius-xl-token=12px` ✓（明/暗）。
- AC3：`--brand`=light `rgb(15,118,110)`#0f766e / dark `rgb(45,212,191)`#2dd4bf ✓；`--amber`=light `rgb(180,83,9)`#b45309 / dark `rgb(251,191,36)`#fbbf24 ✓；legacy `--indigo`→brand teal ✓、`--marketing-accent`→amber ✓；`--marketing-hero-bg=none`（glow 移除）✓；**真实 `text-marketing-accent` 元素（16 处）computed color 实测为 Amber**（light rgb(180,83,9) / dark rgb(251,191,36)）✓。
- AC7：下拉主题切换 light→dark 实测 `data-theme` 由 light 变 dark ✓；ZH `/zh`（bodyText 1799）、QuickStart `/start`（1603）、Why `/why`（2223）均渲染内容 ✓；控制台/页面 0 未捕获错误 ✓。

**AC7 人工目测（双主题全屏截图）**：
- 截图：`test-results/issue5/home-light.png`、`home-dark.png`、`zh-home.png`。
- **Light**：Teal 强调文本/CTA、Amber 徽章（PRE 1.X / DESIGN PARTNER）、对比表 Teal/Amber 勾选、架构管线卡片圆角利落、QuickStart 代码块等宽呈现、Hero 无 radial glow。
- **Dark**：暖中性深底，亮 Teal 强调、Teal CTA 按钮**深色文字清晰可读**（`--primary-fg:#0e1117` 覆盖生效，验证 ui-designer/审查修复有效）、Amber 徽章/图表条/"Be early"卡片 Amber 边框，无掉色、无丢边框、切换无闪烁回归。
- 结论：明暗双主题关键区块（Hero / CrossSignalDemo / vs-表 / One-storage 管线 / QuickStart / DP CTA / 导航 / 页脚）均"利落+克制"，无视觉崩坏。

### 4. 缺陷清单
**无**。本轮 0 缺陷（含 0 阻断 / 0 严重 / 0 一般）。代码审查阶段已直接修复的 dark `--amber-fg` 漏覆盖（commit 487bbba）经本轮 a11y 26/26 + 暗色按钮可读性目测复核，确认闭合。

### 5. 回归套件位置与重跑命令
- 新增 E2E：`tests/e2e/issue5-ui-tokens.mjs`（29 断言，明暗双主题，可一键重跑，幂等）。
- 重跑（PATH 需含 node v23.6.1）：
  ```bash
  export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
  pnpm build && PORT=3217 pnpm start &   # locale: EN=/ ZH=/zh；若 3217 被占换端口
  BASE=http://127.0.0.1:3217 node tests/e2e/issue5-ui-tokens.mjs
  ```
- 静态门禁回归：`pnpm a11y:contrast && pnpm check && pnpm build`。

### 6. 放行建议
**建议放行（VERDICT: PASS）**：AC1–AC8 全部实测通过——a11y 26/26、check/build exit 0、E2E 29/29、双主题截图目测无崩坏、隔离性 git 可核。Geist Mono 真实加载并应用于 mono 元素，83/94 处旧工具类经值重指向整体切换 Teal/Amber 零掉色，明暗按钮前景可读。无阻断缺陷，建议合并并放行 T09/T10/T08b 后续组件改造。

---

## 自动化测试 — ISSUE-6 [T03] 成本计算器/对比表数字校准（P0, full 道）

> 执行者：qa-automation｜日期：2026-06-02｜被测分支：`feature/ISSUE-6`

### 1. 测试范围
- **接口/契约层（静态 + 单测脚本）**：`lib/cost-formula.ts` 的定价常量校准、全输入域无 NaN/无溢出、敌意输入净化、`formatUsd` 守卫、代表点节省比；EN/ZH 文案键 parity。
- **E2E 浏览器层（真实 Chromium，production build）**：`/why`（EN）与 `/zh/why`（ZH）成本计算器：
  - 默认负载（100GB/天 · 30天）渲染校准后的代表点 **Datadog $7.4k / molesignal $150 / 节省 98%**；
  - disclaimer 显示**固定快照月份**（June 2026 / 2026年6月，非访问当天）、"15天索引"口径、来源外链可见且 `href=https://www.datadoghq.com/pricing/` + `target=_blank` + `rel` 含 `noopener`；
  - 滑块拉到极值 + 数字输入清空/敌意输入（`""`/`abc`/`1e`/`-50`）后页面**绝不出现 `NaN`/`$NaN`/`Infinity`**。

### 2. 执行环境与启动方式
- Node v23.6.1（nvm）/ pnpm 11.5.0 / Next 16.2.6 / @playwright/test 1.60（Chromium）。
- 静态与单测：`pnpm typecheck` / `pnpm lint:i18n` / `pnpm test:cost`。
- E2E：`pnpm build` 后 Playwright 自动 `next start -p 3210` 运行 spec（与上线行为一致）。
- 测试结束已 kill 3000/3210 端口进程，无僵尸残留。

### 3. 结果汇总（全部通过）
| 检查 | 命令 | 结果 |
|---|---|---|
| 类型检查 | `pnpm typecheck` | exit 0 |
| 成本公式单测（16 断言：快照/来源、全域扫描峰值$2.01M有界、敌意输入、formatUsd、代表点$7.4k/$150/98%） | `pnpm test:cost` | **全部通过** |
| i18n parity（含新增 disclaimerSourcePrefix/disclaimerSource） | `pnpm lint:i18n` | 506↔506 OK |
| 生产构建 | `pnpm build` | exit 0 |
| **ISSUE-6 E2E（新增，6 用例 EN×3+ZH×3）** | `playwright test issue6-cost-calculator` | **6 passed** |
| **全量 E2E 回归（含 cost_calculator_interact 漏斗用例）** | `playwright test` | **26 passed** |

E2E 用例清单（均 PASS，EN 与 ZH 各一组）：
1. 默认负载渲染校准代表点 $7.4k / $150 / 98%；
2. disclaimer 固定快照月份 + 15天索引口径 + 来源外链（href/target/rel 断言）；
3. 滑块极值 + 数字输入清空/敌意输入后无 NaN/$NaN/Infinity。

### 4. 缺陷清单
**无**。0 阻断 / 0 严重 / 0 一般。代码审查阶段直修的快照日期 `timeZone:"UTC"`（commit 63dc615）经 ZH 渲染实测确认输出 "2026年6月"、EN "June 2026"，水合一致、无时区漂移。

### 5. 回归套件位置与重跑命令
- 新增 E2E：`tests/e2e/issue6-cost-calculator.spec.ts`（6 断言组，EN/ZH 双语，幂等，可一键重跑）。
- 单测脚本：`scripts/check-cost-formula.ts`（`pnpm test:cost`）。
- 重跑（PATH 需含 node v23.6.1）：
  ```bash
  export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
  pnpm test:cost && pnpm lint:i18n
  pnpm build && pnpm exec playwright test issue6-cost-calculator --reporter=list
  # 或全量回归：pnpm exec playwright test
  ```

### 6. 放行建议
**建议放行（VERDICT: PASS）**：验收标准全部实测通过——Datadog 定价校准为 2026-06-02 快照公开值、快照日期/来源 disclaimer 真实渲染且外链可点、全输入域无 NaN/溢出、EN/ZH 同步；6 个新增 E2E + 20 个既有 E2E 全绿，无回归、无阻断缺陷。

## 自动化测试 — ISSUE-7 [T07] GitHub stats/changelog 真实性兜底标注（P0, light 道）

> 执行者：qa-automation｜日期：2026-06-02｜被测分支：`feature/ISSUE-7`。本工单为「核验既有三兜底态 UI + 加离线回归测试」型，生产渲染逻辑未改动（diff 仅新增 `scripts/check-github-fallback.ts` + `package.json` 一行 script）。验证手段：`next dev` 真实 SSR 渲染 + curl 抓 HTML 断言 + Playwright(Chromium) 截图 + 离线回归脚本实跑 + 直接探活 GitHub API。

### 1. 测试范围
- **数据层契约（离线回归）**：`getRepoStats / getReleases / getContributors` 在 network throw / 非 2xx(403/404) / 200 三态下的返回；draft 过滤、bot 过滤、tag/version 解析、`formatStars/timeAgo` 不误导。
- **UI 消费（运行时 E2E）**：① stats chip 三尺寸（nav/hero chip、large）；② changelog live/previewing pill（EN+ZH）；③ 贡献者墙空态（EN+ZH）；④ 全程无伪造数字断言。
- **真实数据切换（现场）**：探活 GitHub API 证实 `molesignal/molesignal` 已公开，验证同代码自动消费真实值。

### 2. 执行环境与启动方式
- node v23.6.1（PATH 需含 `$HOME/.nvm/versions/node/v23.6.1/bin`），pnpm 11.5.0，Next.js 16.2.6。
- `PORT=3737 pnpm dev`（后台），测后已 kill、端口 3737/3738 实测 down、无僵尸 `next dev` 进程。

### 3. 结果汇总
| 项 | 手段 | 结果 |
|---|---|---|
| 离线回归 `pnpm test:github` | tsx 实跑 | **22/22 断言全过** |
| changelog pill（EN /changelog） | SSR HTML 断言 + 截图 | `previewing` 灰 pill ✓ |
| changelog pill（ZH /zh/changelog） | SSR HTML 断言 | 「预览」灰 pill ✓ |
| changelog 回退静态内容 | HTML 断言 | v0.7.0/0.6.0/0.5.0 正常渲染、非空态 ✓ |
| stats chip 真实数据态 | HTML + 截图 | `★ 0 · —`（真实 0 star，— 未知）✓ |
| stats chip 兜底文案态 | 离线测试 + 代码核验 | `★ Star on GitHub` 无数字 ✓ |
| 贡献者墙空态（EN/ZH） | 运行时 403→[] 实测 | 「Be the first contributor」/「成为第一个贡献者」✓，无伪造头像 |
| 全程无伪造数字 | 多次 HTML 断言 | 非零 star span 命中恒 0 ✓ |
| 真实数据自动切换 | GitHub API 探活 | 仓 public/0★/0release，自动消费真实值 ✓ |

**通过 9/9，失败 0。**

### 4. 缺陷清单
**无阻断 / 无严重 / 无一般。** 三处观察（均不阻断、不违反 AC）：
1. ⚠️ 工单"遗留/说明"前提过时：原称仓库未公开呈纯兜底；实测仓库**已公开**（0★/0release），stats chip 实呈真实数据态（`★ 0 · —`），changelog "previewing" 系「0 条已发布 Release」所致而非私有。行为正确诚实，仅工单文字需校正。
2. 未鉴权限流（60/h）下 repo-metadata 200 而 commits 403 时，chip 呈「真实星数 + last-commit 为 —」部分数据态（诚实）。数据层既有行为、范围外。**建议**构建/生产配 `GITHUB_TOKEN` 消除限流抖动。
3. `formatStars` 文档注释 1M vs 实际 "1.0M"——code-reviewer 已记录，范围外。

### 5. 回归套件位置与重跑命令
- 离线回归脚本：`scripts/check-github-fallback.ts`（全 stub fetch，无需密钥/活仓，幂等可一键重跑）。
  ```bash
  export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
  pnpm test:github   # 22/22：throw/!ok→兜底、200→真实数据切换、draft/bot 过滤、formatter
  ```
- 真实环境联调复验（日后配 token + 有 Release 后）：直接访问 `/changelog`（pill 应翻 brand 蓝「live from GitHub」）、首页 chip（应显真实星数与 last-commit）、贡献者墙（应显真实头像）。

### 6. 放行建议
**VERDICT: PASS — 放行合并。** 三兜底态在真实浏览器/SSR 中均被正确消费、零伪造；真实数据自动切换已现场验证；新增离线回归 22/22 全绿。无正确性/契约/安全风险，无阻断缺陷。

---

### ISSUE-8 [T01] Cloud候补真入库（Resend Audience）— 2026-06-02

**结论：VERDICT PASS**（本期 AC1–AC7 全过；AC8 真实 Resend 联调按密钥政策延后，不阻塞 DONE）。

#### 1. 测试范围
- **单测（mock fetch）**：`lib/resend-audiences.ts#addContact` —— 成功(200/201)、幂等(409 + "already exists" 体)、缺 audienceId 跳过、缺 RESEND_API_KEY 跳过、运行时 fail-soft、密钥不入日志+body 截断、T02 姓名映射（`pnpm test:audiences` 26 断言）。
- **接口（真实 HTTP，`next start` 生产构建）**：`POST /api/cloud-waitlist` —— AC2 真入库接线、AC3/AC4 缺 env 优雅降级、AC5 安全（请求体不能覆盖 audienceId/key、密钥不入日志）、输入校验（非法 email / 蜜罐 / 非法 JSON）。
- **E2E（Playwright）**：AC6 前端埋点时序 —— `waitlist_submit` 仅 2xx 后触发，429/蜜罐不触发；全 11 事件漏斗回归不退化。
- **构建/类型/回归（AC7）**：`pnpm check` + `pnpm build`。
- **未覆盖**：AC8 真实向 Resend audience 写入并在 Dashboard 可见/可导出（本期无 `RESEND_API_KEY`/`RESEND_CLOUD_AUDIENCE_ID`，延后补密钥复验）。

#### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0 / Next.js 16.2.6；生产构建 `pnpm build` 后 `pnpm start -p 3199`（Ready 69ms）。
- **AC2 取证手法**：注入伪造 `RESEND_API_KEY=re_fake_...` + `RESEND_CLOUD_AUDIENCE_ID=aud_fake_cloud_999` 重启 → 提交合法 email，观察服务端日志确认路由**确实发出**了向 audience 端点的 POST（伪 key 被 Resend 拒为 400，证明请求已发出），且响应仍 200（fail-soft）。
- AC3/AC4 分别以"仅 key 无 audience id""无任何 RESEND env"重启复测降级。
- E2E：`pnpm exec playwright test`（自起 `next start -p 3210`，stub `window.plausible` 录事件）。

#### 3. 结果汇总

| AC | 验证点 | 手段 | 结果 | 证据 |
|----|--------|------|------|------|
| AC1 | provider 幂等（409 / already-exists → 成功不抛） | 单测 mock fetch | ✅ | test:audiences「[AC1] 409→ok / 422 already-exists→ok」 |
| AC2 | 真入库接线（配齐 env → 发出 audience POST + 仍发通知，200） | 真实 HTTP + 日志 | ✅ | `[audiences] resend failed 400 API key is invalid`（请求已发出）+ `[email] resend failed 401`（通知亦触发）+ HTTP 200 |
| AC3 | 缺 audience id → 跳过入库/仍通知/200/warn | 真实 HTTP + 日志 | ✅ | `[audiences] audience id missing — skipping list insert` + 通知触发 + HTTP 200 |
| AC4 | 缺 RESEND_API_KEY → 入库+通知皆 skip/200/warn/不抛 | 真实 HTTP + 日志 | ✅ | `[audiences] RESEND_API_KEY missing` + `[email] ... skipping send` + HTTP 200 |
| AC5 | PII/安全：env-only 不可被请求体覆盖、密钥不入日志、蜜罐不入库 | 真实 HTTP + 日志 + 单测 | ✅ | 注入 `audienceId:"attacker_aud"` 未出现在任何请求；日志无完整 key、body 截断；蜜罐 `website` 非空 → 400（不入库/不通知/不 track） |
| AC6 | 前端 2xx 后才 track，429/蜜罐不 track，全漏斗不退化 | Playwright E2E | ✅ | waitlist 3/3 + 全漏斗 14/14 通过 |
| AC7 | `pnpm check` + `pnpm build` exit 0；新单测全过 | 构建/脚本 | ✅ | check exit 0（lint+typecheck+a11y 26/26+i18n 506↔506）；build exit 0；test:audiences 26/26 |
| AC8 | 真实 Resend 写入可见/可导出 | — | ⏸ 延后 | 本期无密钥，按密钥政策延后复验，不阻塞 DONE |

辅助接口断言：非法 email → 400；非法 JSON → 400；蜜罐 `website` 命中 → 400（zod，结果上满足"不入库/不通知/不 track"）。

#### 4. 缺陷清单
- 无阻断级缺陷。
- 观察（非阻断，非本工单引入）：蜜罐 `website` 为 `z.string().max(0)`，非空即 zod 400，早于 route 内 silent-200 分支（该分支为死代码）；结果上安全要求满足。开发/审查均已如实标注，统一为 silent-200 应另开工单。

#### 5. 回归套件位置与重跑命令
```bash
export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$HOME/Library/pnpm:$PATH"
cd workspace/molesignal-website
pnpm test:audiences        # provider 单测 26 断言（AC1/AC3/AC4/AC5 + fail-soft + T02）
pnpm check && pnpm build   # AC7 门禁
pnpm exec playwright test tests/e2e/analytics-funnel.spec.ts -g waitlist   # AC6
# AC2/AC3/AC4 真实 HTTP 降级：本期手工以伪 env 起 `pnpm start` + curl 取证（见 §2）
```

#### 6. 放行建议
**放行（PASS）**。本期 AC1–AC7 全部客观通过：provider 幂等与三降级、route 真入库接线与 fail-soft、PII/密钥安全、前端埋点时序、构建门禁均经真实执行验证。AC8 真实 Resend 联调按既定密钥政策延后，不阻塞本期 DONE。

---

## 代码审查 — ISSUE-10 [T09] 核心页面视觉改造 (Hero/Demo/Why-Stats/Start/DP CTA)

审查人：code-reviewer ｜ 日期：2026-06-02 ｜ LANE: light ｜ 分支：feature/ISSUE-10 ｜ 改动：`app/[locale]/page.tsx`、`components/cross-signal-demo.tsx`、`components/github-stats-chip.tsx`、`components/ui/pill.tsx`、`messages/{en,zh}.json`

### 结论
**必改 0｜建议放行 QA。** 审查中发现 2 处「类名不编译」缺陷（hover 失效 + brand pill 无底色），均已由审查员在本工单内直接修复并复测全绿。另发现 1 处全局根因（设计 token 桥接缺失），属本工单之外的存量问题，已记录建议给架构师/前端单开工单处理。

### 关键发现与处置

| # | 位置 | 类型 | 级别 | 说明 | 处置 |
|---|------|------|------|------|------|
| F1 | `app/[locale]/page.tsx:61` & `:185` | bug / 回归 | 建议改 | 两处主 CTA 由 `hover:shadow-glow-indigo`（可编译、原本生效）改为 `hover:bg-primary-muted`。但 `--color-primary-muted` 未注册进 `@theme inline`，Tailwind v4 **不生成** `bg-primary-muted` 工具类 → 两个主按钮 hover **完全无视觉反馈**（`transition-colors` 无目标）。经全新 `next build` 比对编译产物确认：`.shadow-glow-indigo` 存在，`.bg-primary-muted`/`hover:bg-primary-muted` 缺失。AC §1 要求「hover 色变」实际未达成。 | **已修**：改为 `hover:bg-primary/90`（`--color-primary` 已桥接 → 可编译，含 opacity color-mix，与既有 `bg-marketing-accent/70` 同款渐进增强，hover 有可见色变）。 |
| F2 | `components/ui/pill.tsx:12`（被 `page.tsx:130` 新 "5 minutes" pill 触发） | bug | 建议改 | `brand` variant = `bg-primary-bg text-primary`，`bg-primary-bg` 同样未桥接 → 不编译 → brand pill **无底色**，仅剩裸文字，与同组 `marketing`（`bg-marketing-accent-dim`，可编译）视觉权重不一致。AC §4「brand pill」实际未达成。该 variant 另有 3 处消费方（start/roadmap/architecture），同样受影响。 | **已修**：brand variant 改为 `bg-primary/10 text-primary`（可编译的 10% 主色淡底，4 处 brand pill 一并修好）。 |
| F3 | `app/globals.css`（`@theme inline` 桥接层） | 坏味道 / 根因 | 建议改（本工单外） | F1/F2 同源：`:root` 定义了语义 token `--primary-bg`/`--primary-muted`，但**未桥接**为 `--color-primary-bg`/`--color-primary-muted`，导致 `bg-primary-bg`/`bg-primary-muted`/`border-primary-muted` 全站 8+ 组件（compare-table、pricing、design-partner-form、cost-calculator、architecture-diagram、why、pre-release-banner、github-stats-chip）静默失效。 | **未改（建议交回）**：根因修法是在 `@theme inline` 补 2 行别名 `--color-primary-bg: var(--primary-bg);` `--color-primary-muted: var(--primary-muted);`。但此举会让 8+ 组件「恢复」原本缺失的底色/边框，属跨多模块视觉变更，需前端/架构师**配合全量视觉与 a11y 重核**（建议升 full 单独处理），不在本 light 工单 silently 改。 |

### 合规核对（其余均通过）
- **新增工具类编译性逐一核对**（全新 build 后查编译 CSS）：`text-primary`/`text-primary-foreground`/`border-l-primary`/`bg-bg-2`/`bg-red·green/70`/`bg-marketing-accent·-dim`/`text-marketing-accent`/`text-mono-2xl` 全部生成 ✓。
- **契约/i18n**：新增 `hero.command`、`home.startPill`（EN/ZH 双侧齐），`generateMetadata` 仍引用 `hero.subtitle`（未变成死键）✓；`crossSignalDemo.pill` 键保留但不再渲染（开发已注明，为 i18n parity，无副作用，可选清理）。
- **可访问性**：三点 chrome `aria-hidden` ✓；tablist/tab/`aria-selected`/`aria-live` 角色保留 ✓；active 下划线 span `aria-hidden` ✓。
- **正确性/资源**：无并发/泄露问题；`AnimatePresence`/`useReducedMotion` 逻辑未动。
- **提交规范**：2 次提交均 Conventional Commits 且带 `(ISSUE-10)` 工单号 ✓。

### 复测（审查员修复后，全新执行）
- `tsc --noEmit` → rc=0
- `next build` → rc=0；`hover:bg-primary/90` 与 `bg-primary/10` 均含 opacity color-mix 规则，确认生效
- `eslint .` → 0 error（仅 `toc.tsx` 5 条既有 warning，非本工单）
- `a11y:contrast` → 26/26 AA 全过
- `lint:i18n` → EN/ZH 508↔508 OK

### 放行建议
**建议放行 qa-automation 独立验证**（浏览器 E2E + 明暗双主题截图，重点复核：Hero 左对齐/mono $ 命令/docker 预览、terminal 三点+3px 下划线+amber trace_id、5min brand pill 现在有底色、两 CTA hover 现在有色变、DP CTA 左 border 卡）。F3 根因建议另开工单。

---

## 自动化测试 — ISSUE-10 / T09 核心页面视觉改造（qa-automation, 2026-06-02）

> 独立于代码审查，由 qa-automation 起真实生产服务 + 真浏览器执行验证。

### 1. 测试范围
- **E2E（新增回归）**：`tests/e2e/issue10-t09-visual.spec.ts` —— 6 项 AC × 2 locale（en/zh）= 12 用例，断言真实 DOM/computedStyle：
  - AC1 Hero 左对齐(`.max-w-4xl`)、高亮 `text-primary` 且 `background-image:none`（确认非渐变）、mono `$ 命令` 副标题（fontFamily 含 mono、`$` 为 brand 色）
  - AC2 真实 `docker compose … --profile standalone up` 命令预览可见
  - AC3 demo terminal：红/amber/绿三点各 1、trace_id code 计算色为暖色 amber(R>B,G>B)、active tab `.bg-primary.h-[3px]` 下划线可见
  - AC4 large stats chip 数字 `span.text-primary.font-mono.text-mono-2xl`（fontFamily 含 mono）
  - AC5 DP CTA `.border-l-primary` 左边框宽≥3px、非透明、box-shadow 不含粉色 glow；5min brand pill 可见
  - AC6 暗色主题下 Hero 高亮/DP 卡/mono 副标题均可见且文字色非透明
- **既有回归**：analytics-funnel(14) + issue4-legal(6) + issue6-cost-calculator(6) 全套 .spec.ts。
- **UI token 回归**：`tests/e2e/issue5-ui-tokens.mjs`（明暗双主题 token 真渲染，与本次 token 改动强相关）。

### 2. 执行环境与启动方式
- Node v23.6.1 / pnpm 11.5.0（corepack）；生产构建 `pnpm build`(rc=0) 后 `next start`。
- Playwright 套件：`pnpm exec playwright test`（webServer 自动起 `next start -p 3210`，light 主题、reducedMotion）。
- token 脚本：`next start -p 3217` 后 `BASE=http://127.0.0.1:3217 node tests/e2e/issue5-ui-tokens.mjs`（测毕已 kill，无僵尸）。

### 3. 结果汇总
| 套件 | 用例 | 结果 |
|---|---|---|
| 质量门 `pnpm check`（tsc/eslint/a11y/i18n） | — | rc=0，0 error，a11y 26/26，i18n 508↔508（5 条 toc.tsx 既有 warning） |
| `pnpm build` | — | rc=0 ✓ |
| Playwright 全量 .spec.ts | **38** | **38 PASS / 0 FAIL** |
| ├ 新增 issue10-t09-visual（en+zh × 6 AC） | 12 | 12 PASS |
| ├ analytics-funnel | 14 | 14 PASS |
| ├ issue4-legal | 6 | 6 PASS |
| └ issue6-cost-calculator | 6 | 6 PASS |
| UI token 脚本 issue5-ui-tokens.mjs | 29 | **29 PASS / 0 FAIL**（含 `--marketing-hero-bg === none` glow 已移除、brand/amber token 明暗值正确、Geist Mono 真加载） |
- **总计 67 项自动化断言全过，0 失败。**
- 真浏览器截图（人工可复核）：`test-results/issue5/home-light.png`、`home-dark.png`、`zh-home.png`。截图确认：Hero 左对齐 + "Without the Datadog bill." brand teal 高亮（无渐变）、mono `$` 副标题、docker 命令卡、terminal 三点 + amber trace_id、"Five minutes" pill、DP CTA 左边框 accent 无 glow。

### 4. 缺陷清单
- **本工单范围内：无缺陷（0）。** 6 项 AC 在 en/zh × 明暗主题下全部命中。
- 既有遗留（非本工单，开发/审查已记录、不阻断放行）：① `toc.tsx` 5 条 eslint warning（存量）；② F3 根因 `@theme inline` 缺 `--color-primary-bg`/`--color-primary-muted` 桥接致部分 `bg-primary-bg/-muted` 工具类站点静默失效——属跨模块存量问题，审查已建议另开 full 工单，T09 已规避（改用 `bg-primary/10`、`hover:bg-primary/90` 等可编译写法，token 脚本验证 marketing-hero-bg/brand/amber 均正常）。

### 5. 回归套件位置与重跑命令
```bash
# 前置：加载 node 工具链
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; corepack enable
cd molesignal-website && pnpm build           # E2E 跑生产构建

# T09 专项 + 全量 E2E（webServer 自动起 next start:3210）
pnpm exec playwright test issue10-t09-visual  # 仅 T09 12 用例
pnpm exec playwright test                     # 全量 38 用例

# UI token 回归（需手动起服务）
pnpm exec next start -p 3217 & \
BASE=http://127.0.0.1:3217 node tests/e2e/issue5-ui-tokens.mjs
```

### 6. 放行建议
**PASS — 建议放行合并。** 6 项 AC 真浏览器全绿、全量回归 0 失败、构建与质量门 rc=0、明暗双主题与 en/zh 双语均验证通过，无阻断缺陷。F3 token 桥接根因建议另开 full 工单跟进（不阻断本工单）。

**VERDICT: PASS**

---

## 代码审查 — ISSUE-11 / T10 表单页+成本/对比视觉改造

- 审查人：code-reviewer ｜ 日期：2026-06-02 ｜ LANE: light ｜ 分支：feature/ISSUE-11
- 改动文件：`components/cost-calculator.tsx`、`components/compare-table.tsx`、`components/cloud-waitlist-form.tsx`、`components/design-partner-form.tsx`、`app/[locale]/design-partner/page.tsx`、`messages/{en,zh}.json`

**结论：必改 1（审查员已直接修复并构建验证）｜建议放行 QA。** 发现 1 处「类名不编译」缺陷（与 ISSUE-10 F1/F2 同根因），导致本工单 3 项 AC 的品牌底色/边框/圆形实际不渲染，已在本工单内直接修复并经生产构建产物比对确认生效。其余正确性/契约/i18n/质量门均通过。

### 缺陷清单

| # | 位置 | 类型 | 级别 | 说明 | 处置 |
|---|---|---|---|---|---|
| F1 | `cost-calculator.tsx:222`（molesignal 卡 `tone="brand"`） | bug / 回归 | **必改** | 本工单将 molesignal 结果卡从 `tone="good"`（`bg-green-dim`，**可编译**、原有绿底）重映射为 `tone="brand"`（`border-primary-muted bg-primary-bg`）。但 `--color-primary-bg`/`--color-primary-muted` 未注册进 `@theme inline`（globals.css 91-92 仅有原始 `--primary-bg/-muted`，无 `--color-` 桥接），Tailwind v4 **不生成** `bg-primary-bg`/`border-primary-muted` → 该「我们更便宜」中心卡片**无底色、无边框**，与红色 Datadog 卡、amber Savings 卡视觉权重不一致。AC「molesignal=brand teal」实际未达成。生产构建产物比对：编译 CSS 中 `bg-primary-bg` 出现 **0 次**。 | **已修**：`tone="brand"` → `border-primary/30 bg-primary/10`（`--color-primary` 已注册→可编译，含 opacity color-mix，沿用 ISSUE-10 既定修法）。 |
| F2 | `app/[locale]/design-partner/page.tsx:98`（DP 三步圆形） | bug | **必改** | 步骤数字圆形沿用 `bg-primary-bg`（同 F1 不编译）→ AC「brand-dim 圆形」**无填充**，仅剩裸数字。 | **已修**：`bg-primary-bg` → `bg-primary/10`。 |
| F3 | `compare-table.tsx:73`（molesignal 列头） | bug | 建议改 | 本工单为该列头新增 `border-t-brand`（可编译、品牌顶边正常显示✓）；但同一行既有 `bg-primary-bg` + `ring-primary-muted/30`（同 F1 不编译）→ 品牌列底色/描边不渲染，与新顶边设计割裂。底色为存量问题但属本工单强化的同一组件。 | **已修**：`bg-primary-bg`→`bg-primary/10`、`ring-primary-muted/30`→`ring-primary/30`（顶边写法不动）。 |

### 验证（修复后全绿）
- `tsc --noEmit` rc=0；`eslint` rc=0（仅 `toc.tsx` 5 条存量 warning，本工单 0）；`a11y:contrast` 26/26 达 WCAG AA；`i18n:parity` EN/ZH 511=511 OK。
- `next build` rc=0；编译 CSS 产物比对：`var(--color-primary) 10%`、`30%` 的 color-mix 规则**已生成**（修复生效），`bg-primary-bg` 字面工具类**仍为 0 次**（佐证原写法不编译）。

### 其他观察（不阻断）
- **正确性/契约 OK**：两表单 429 处理逻辑正确——onSubmit 起始 `setRateLimited(false)`、命中 429 即 `setRateLimited(true)` 并 `return`（不计入 `track()` 转化），注释已同步更新为「5xx/network errors fall to catch」。429 展示态依赖后端返回 HTTP 429（T11/Upstash 提供），前端仅展示，契约一致。
- **i18n**：新增 `cloud.form.successBody`/`cloud.form.rateLimit`/`designPartner.form.rateLimit` EN↔ZH 双侧齐，parity 校验通过，无死键。
- **可选（坏味道）**：`cloud-waitlist-form` 抽出了 `RateLimitNotice` 组件，而 `design-partner-form` 的 429 提示仍为内联 `<p>`（class 与组件版基本一致）——可后续抽公共组件去重，非必改。
- **可选（存量）**：`CloudWaitlistForm` 的 `tone="footer"` 分支（含其 success/rateLimited 态）当前无任何调用方（站内仅 `/cloud` 用默认 tone），属既有未用 API 面，非本工单引入。
- **存量根因（强烈建议另开工单）**：F1/F2/F3 同源——`@theme inline` 缺 `--color-primary-bg`/`--color-primary-muted` 桥接，全仓 8+ 站点（`why`/`pricing`/`pre-release-banner`/`github-stats-chip`/`architecture-diagram` 等）仍用失效的 `bg-primary-bg`/`bg-primary-bg/40`/`group-hover:bg-primary-muted`。本工单按 ISSUE-10 先例仅修受影响的 3 处，建议另开 full 工单在 globals.css `@theme` 注册这两个 token 以一次性根治（届时可统一回归 `bg-primary-bg` 语义命名）。

### 放行建议
**建议放行 qa-automation 独立验证**（浏览器 E2E + 明暗双主题截图，重点复核：CostCalc 三卡语义色——Datadog 红/molesignal 现有 teal 底+边/Savings amber 大数 mono；滑块 44px 触摸区；CompareTable brand 顶边 + ✓✗~ mono 字形 + brand 列底色；表单成功持久绿卡 + 429 amber 温和提示；DP 三步圆形现有 brand 底 + dashed 连接线）。

---

## 自动化测试 — ISSUE-11 / T10 表单页+成本/对比视觉改造（qa-automation, 2026-06-02）

- 执行人：qa-automation ｜ 日期：2026-06-02 ｜ LANE: light ｜ 分支：feature/ISSUE-11 ｜ 提交：33b4a25
- **结论：VERDICT: FAIL** —— 1 处 **Minor** 缺陷（DEF-1：对比表 ✓✗~ 标记实际未渲染为等宽字体，与 AC「✓✗mono」不符）。其余全部 AC 真浏览器实测通过，含本工单核心回归风险点（品牌底色/边框真实渲染）已验证修复。

### 1. 测试范围
| 维度 | 覆盖 |
|---|---|
| 静态质量门 | `pnpm check`：typecheck 0 错、eslint 0 错（仅 toc.tsx 5 条存量 warning）、a11y 对比度 26/26 达 WCAG AA、i18n parity EN/ZH 511=511 |
| 生产构建 | `pnpm build` rc=0（Next 16.2.6 / Turbopack） |
| E2E（新增 `tests/e2e/issue11-t10-visual.spec.ts`，真 Chromium / `next start` 生产渲染） | CostCalc 三结果卡语义色 + brand 卡真实底色/边框 + Savings amber mono 大数 + 滑块 44px；CompareTable brand 顶边(3px)+真实 tint + ✓✗ mono 字形；CloudWaitlistForm 成功绿卡 + 429 amber 温和提示（route mock 驱动）；DesignPartnerForm 成功绿卡 + 429 amber 保留表单；DP「三步」mono brand 圆形徽标 |
| 全量回归 | `pnpm exec playwright test` 全套（issue6 成本表/issue10 首页视觉/issue4 法务/issue5 UI token/issue3 死链/analytics 漏斗 等）|

### 2. 执行环境与启动方式
```bash
# Node 经 nvm（v23.6.1），项目用 pnpm
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use default
pnpm check          # 质量门
pnpm build          # 生产构建
pnpm exec playwright test tests/e2e/issue11-t10-visual.spec.ts   # 本工单 E2E
pnpm exec playwright test                                        # 全量回归（webServer 自起 next start :3210）
```

### 3. 结果汇总
- **质量门**：typecheck ✅ / eslint ✅ / a11y 26/26 ✅ / i18n 511=511 ✅
- **构建**：`next build` rc=0 ✅
- **本工单 E2E（issue11-t10-visual.spec.ts）**：**10 通过 / 2 失败**（2 失败均为 DEF-1，分别在 `/why` 与 `/` 两页的同一断言）
- **全量回归**：**48 通过 / 2 失败**（2 失败=DEF-1；其余既有回归套件 0 回退，无附带破坏）

| AC 项 | 用例 | 结果 | 证据 |
|---|---|---|---|
| CostCalc 三卡语义色 + **brand 卡真实底色/边框**（ISSUE-10/11 核心回归点） | AC1 cards | ✅ PASS | canvas 解析 computed `background-color`/`border-top-color` 均非透明（`bg-primary/10`、`border-primary/30`、`bg-red-dim`、`bg-amber-dim` 全部真实上色） |
| Savings amber + mono 大数 | AC1 savings | ✅ PASS | computed fontFamily 含 mono；色彩暖调（R>B、G>B）符合 amber |
| 滑块 44px 触摸区（WCAG 2.5.5） | AC1 slider | ✅ PASS | `input[type=range]` boundingBox.height ≥ 44 |
| CompareTable brand 顶边(3px)+真实 tint | AC2 header | ✅ PASS | `th.border-t-brand` computed border-top-width≥3、border-top-color 与 bg 均真实上色 |
| **CompareTable ✓✗~ 等宽字形** | AC2 glyphs | ❌ **FAIL** | computed fontFamily = `"Inter Variable", ui-sans-serif…`（**非 mono**）；见 DEF-1 |
| Cloud 表单 2xx→持久绿卡 | AC3 success | ✅ PASS | `[role=status].bg-green-dim` 可见、真实绿底、含 check 图标（route mock 200） |
| Cloud 表单 429→amber 温和提示且不替换表单 | AC3 ratelimit | ✅ PASS | `.bg-amber-dim` 可见、左 3px 暖色边、`#cw-email` 仍在（route mock 429） |
| DP 表单 2xx→绿卡 | AC4 success | ✅ PASS | DP 表单内 submit 提交后绿卡可见、真实绿底+图标 |
| DP 表单 429→amber 且保留已填内容 | AC4 ratelimit | ✅ PASS | amber 提示可见、`#dp-name` 仍保留输入值 |
| DP「三步」mono brand 圆形 | AC5 steps | ✅ PASS | `ol li > span.bg-primary/10` 计数=3、真实 brand 底、fontFamily 含 mono、首徽=「1」 |

> 测试方法学：所有色彩断言均在浏览器内经 1×1 canvas 把 computed 颜色解析为 sRGB（Tailwind v4 输出 `oklab()`，普通 `rgb()` 正则无法解析），证明底色/边框**真实绘制**而非「类名存在但不上色」——直击 ISSUE-10/11 同源回归风险，结果确认本工单 brand/amber 底色修复全部生效。

### 4. 缺陷清单

| # | 位置 | 类型 | 级别 | 复现步骤 | 预期 vs 实际 | 根因 | 责任模块 |
|---|---|---|---|---|---|---|---|
| **DEF-1** | `components/compare-table.tsx:195-207`（`VerdictIcon`） | bug（AC 未达成 / 类名静默失效） | **Minor（外观，无功能影响）** | 起 `next start`→访问 `/why` 或 `/`→读取对比表任一 ✓/✗/~ 标记 span 的 computed `font-family` | 预期：等宽字体（Geist Mono，AC「✓✗mono」/ 05-UI §3.7「mono 读作数据」）。实际：`"Inter Variable", ui-sans-serif, system-ui, sans-serif`——**非等宽**。全站 64 个 ✓ + 28 个 ✗ 均如此（桌面表格 + 移动卡片、两页一致） | `cls` 串为 `"… font-mono font-strong …"`，经 `cn()`=`twMerge(clsx(...))`。`font-strong`（自定义 `--font-weight-strong`）未被 stock tailwind-merge 识别，被并入与 `font-mono` 同一 `font-*` 冲突组，twMerge **保留靠后者**=`font-strong`，**静默剔除 `font-mono`**。渲染 HTML 实测仅剩 `font-strong`，无 `font-mono`。（对照：Savings 大数 `font-mono` 在末位故生效，可正常渲染 mono——侧证此机制。） | frontend-engineer / compare-table.tsx |

**修复建议（一行级，且本质同 ISSUE-10/11「让类真正生效」）**：将 `VerdictIcon` 的 `cls` 中 `font-mono` 调到 `font-strong` 之后（`"… font-strong font-mono leading-none"`），或直接用 `[font-family:var(--font-mono)]` 任意值类绕过 twMerge 冲突组；亦可给 `cn` 配 `extendTailwindMerge` 声明 `font-strong` 为 font-weight 组以根治全仓同类隐患。修复后重跑 `issue11-t10-visual.spec.ts` 的 AC2 glyphs 两例应转绿。

> 截图证据：`/tmp/compare-table.png`（对比表整表）——可见 brand 顶边/teal 列底/✓绿✗红~amber 语义色均正确；唯字形为 Inter（视觉上 ✓✗ 符号与 mono 差异极小，故定级 Minor，但 computed font 可证 AC 字面未达成）。

### 5. 回归套件位置与重跑命令
- 新增套件：`tests/e2e/issue11-t10-visual.spec.ts`（12 例，覆盖 AC1–AC5，含表单成功/限流 route-mock 驱动 + canvas 色彩解析）。
- 重跑：
```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use default
# 仅本工单
pnpm exec playwright test tests/e2e/issue11-t10-visual.spec.ts
# 全量回归（自动 next start :3210；需先 pnpm build）
pnpm build && pnpm exec playwright test
```

### 6. 放行建议
**VERDICT: FAIL（回流修复 DEF-1）。** 判据：本工单 P0 的核心回归风险——「品牌/amber 底色、边框、圆形真实渲染」——已真浏览器逐项验证修复生效（与 ISSUE-10 同源问题不再复现），全量回归 0 附带破坏；但代码审查特别点名复核的「CompareTable ✓✗~ mono 字形」经 computed-style 实测**确为非等宽**（DEF-1），与该 AC 字面要求不符，且正属本工单主题「类名静默失效」之同类隐患，故按质量门标准回流。缺陷为 Minor（纯外观、零功能影响、视觉差异细微），修复为一行级，预计下一周期可快速转绿。

> 备注（供主管裁量）：若产品侧判定 ✓✗ 字形差异可接受、愿以「记一条 Minor 跟进、本轮放行」处理，可由主管覆盖为 PASS；但纯 QA 闸门视角依 AC 字面，本轮判 FAIL。

**VERDICT: FAIL**

---

## 自动化测试 — ISSUE-11 / T10 复测（第2轮，DEF-1 根因修复验证 / qa-automation 2026-06-02）

> 上一轮 `VERDICT: FAIL`（DEF-1：对比表 ✓✗~ 渲染为 Inter 非等宽）。本轮验证 `lib/utils.ts` 的 cn() 根因修复（`extendTailwindMerge` 注册自定义字重/字号令牌）是否真正修复 DEF-1，并复跑全量回归确认 0 回退。

### 执行环境
- Node v23.6.1 / pnpm，生产构建 `pnpm build` rc=0 → `next start -p 3210`，真 Chromium（Playwright 1.60，light theme，reducedMotion）。
- HEAD `8e259ef`（含 DEF-1 根因修复 `b8af2d8` + 复审 `8e259ef`）。

### 质量门（全绿）
- `pnpm check`：typecheck 0 错 / eslint 0 错 / a11y 对比度 **26/26** WCAG AA / i18n parity **511=511**。
- `pnpm build`：rc=0。

### 结果汇总
| 套件 | 结果 |
|---|---|
| 本工单 `issue11-t10-visual.spec.ts`（12 例） | **12 通过 / 0 失败**（上轮 10/2 → DEF-1 两例 glyphs 已转绿）|
| 全量 Playwright 回归（6 套 .spec.ts，50 例） | **50 通过 / 0 失败**（上轮 48/2 → 0 回退）|
| `issue5-ui-tokens.mjs`（字体/令牌，@3210，29 断言） | **29 通过 / 0 失败**（cn() 改动未伤及 font-mono→Geist Mono 令牌链）|

### DEF-1 根因修复 — 计算样式实证（已修复 ✅）
浏览器内 `getComputedStyle` 逐项读取，证明 family/weight/size/color 四类设计令牌现已共存、不再被 twMerge 静默丢弃：
- **对比表 ✓ 标记**：`font-family = "Geist Mono", ui-monospace…`（**等宽，DEF-1 修复**）且 `font-weight = 600`（`font-strong` 同时保留）—— family+weight 共存，正是根因修复目标。
- **CostCalc Savings 大数**（潜伏缺陷一并修复）：`family=Geist Mono` / `font-size=32px`（display-lg「大数」已放大）/ `font-weight=700`（display-strong）/ `color=rgb(180,83,9)`（amber）—— 4 个类全部生效。
- 截图：`/tmp/issue11-compare-table.png`（molesignal 列 brand teal 3px 顶边 + tint，✓green/✗red/~amber 等宽字形）、`/tmp/issue11-why.png`。

### 缺陷清单
- 无新增缺陷。上轮 DEF-1（Minor）经计算样式与全套件复测确认**已修复关闭**。
- 备注：`issue3-t04-deadlinks.mjs` / `issue5-ui-tokens.mjs` 为其它工单的独立脚本，默认连 :3000 / :3217，直接 `node` 跑会因无服务报连接错误（非回归）；issue5 已指向 :3210 复跑通过，issue3（死链治理）与本 cn() 改动无关，未单独起服务复跑。

### 回归套件位置与重跑命令
```bash
export PATH="$HOME/.nvm/versions/node/v23.6.1/bin:$PATH"
cd <项目>
pnpm check && pnpm build
pnpm exec playwright test issue11-t10-visual   # 本工单 12 例
pnpm exec playwright test                       # 全量 50 例
# 字体/令牌（需 :3210 起服务）: BASE=http://localhost:3210 node tests/e2e/issue5-ui-tokens.mjs
```

### 放行建议
- 本工单 AC（CostCalc 语义色+Savings amber mono 大数+滑块 44px / CompareTable brand 顶边+mono ✓✗~ / 两表单绿成功卡+429 amber 提示 / DP 三步 brand 圆形）经真实浏览器计算样式逐项确认达成；DEF-1 已根治；全量回归 0 回退。**建议放行（PASS）**。
- 测试完成已关闭 :3210 服务（无僵尸进程）。

**VERDICT: PASS**

---

## 代码审查 — ISSUE-12 [T08b] TopNav/Footer/Pill/Button 视觉改造（code-reviewer, 2026-06-02）

**审查范围**：`git diff main...feature/ISSUE-12`，源码改动 5 文件
（`app/globals.css`、`components/layout/top-nav.tsx`、`components/layout/footer.tsx`、`components/ui/button.tsx`、`components/ui/pill.tsx`）。

### 结论速览
- **必改：0　建议改：0　可选：2**
- 关键 bug：无。所有引用到的设计 token / Tailwind 工具类均已确认在 `globals.css` 注册并能解析。
- 自跑闸门（独立复验，非引用开发自述）：`typecheck` 0 错；`lint` 0 错（5 条 warning 均在未改动的 TOC 组件，非本工单引入）；`a11y:contrast` 26/26 全过 AA。
- **放行建议：PASS 到 QA**（浏览器 E2E 仍需 qa-automation 独立跑：active 下划线 computed border、56px 高度、滚动 blur、CTA `active:scale` 按压、Pill 新变体明暗双主题）。

### 正确性 / 契约核验（逐项）
1. **token 引用全部解析成功**（重点核验，token 迁移最易踩空引用）：
   - `shadow-glow-brand`→`globals.css:464`、`shadow-glow-amber`→`465`、`brand-dim`/`amber-dim`/`red-dim`/`green-dim` 及对应 `--color-*` 注册（316/318/321/323/343）均在；
   - `font-mono-strong` 经 Tailwind v4 `--font-weight-mono-strong:600`（`436`）生成工具类，`text-mono-lg`→`427`（15px），`border-brand` 经 `--color-brand`（316）生成——均有效。
2. **active 下划线无布局抖动**：base 态预留 `border-transparent border-b-2`，active 切 `border-brand`，行高不跳；`transition-colors` 覆盖 border-color，过渡平滑。处理正确。
3. **Pill 向后兼容**：`marketing`(amber)/`warning`(red) 作别名保留，现有调用站点（`marketing`/`brand`/`default`，全仓核验）零破坏；新增 `amber`/`danger`/`muted` 为纯增量、TS 类型安全。
4. **与 T04 不冲突**：footer 仅改链接配色（`text-fg`→`text-fg-muted hover:text-fg`，符 §3.4 tx-2/tx-0），四列结构与 `aria-disabled` 禁用态未动。
5. **nav 高度联动**：`--nav-h`/`--spacing-nav` 4rem→3.5rem，下游 `var(--nav-h)` sticky 偏移随之收紧，方向正确。

### 可维护性 / 风格（可选，非阻塞，建议留作后续工单）
- **[可选] 全站 glow 命名债**：`shadow-glow-indigo`/`shadow-glow-pink` 仍有 ~20 处调用站点（pricing/why/architecture/各表单等，均在 T08b 范围外）。它们经 token 别名仍渲染正确的 teal/amber，**不是 bug**；但 canonical 名已是 `brand`/`amber`，建议开一张清理工单统一收口，避免命名双轨长期存在。
- **[可选] `muted` 与 `default` 完全同值**：pill 的 `muted: "bg-surface-muted text-fg-muted"` 与 `default` 逐字符相同，属语义别名；可接受，留注释说明即可。

### 提交规范
- 单 commit `feat(ISSUE-12): TopNav/Footer/Pill/Button 视觉改造 (T08b)`，符合 Conventional Commits，标题带工单号。✓

---

## ISSUE-12 / T08b 自动化测试（qa-automation, 2026-06-02）

> 本节为 qa-automation 独立执行结果，与上方 code-reviewer 静态审查互补。**结论：VERDICT PASS。**

### 1. 测试范围
- **E2E（浏览器，真 Chromium）**：TopNav 56px 高度 / brand 等宽字体 / active 2px brand 下划线（非背景填充）/ 滚动 backdrop-blur；Footer 四列 + T04 禁用态；Pill brand 变体真填充 + 边框 + 文字色；brand CTA 填充；T04 链接契约。
- **回归**：T04 死链治理（issue3）、UI token 双主题（issue5）。
- 接口层：本工单纯前端视觉改造，无 API 出入参变更，无接口测试范围。

### 2. 执行环境与启动方式
- `pnpm build`（rc=0，全部迁移 token 编译通过，是 token-未编译类回归的第一道证伪）→ `next start -p 3210` 生产渲染。
- Playwright 1.60 / Desktop Chrome，颜色断言走 canvas 把 oklch/oklab 转 sRGB 读 computed style（避免类名假阳性）。

### 3. 结果汇总
| 套件 | 文件 | 结果 |
|---|---|---|
| T08b 视觉 E2E（新增） | `tests/e2e/issue12-t08b-visual.spec.ts` | **9/9 PASS** (8.1s) |
| T04 死链回归 | `tests/e2e/issue3-t04-deadlinks.mjs` | **13/13 PASS** |
| UI token 回归 | `tests/e2e/issue5-ui-tokens.mjs` | **29/29 PASS** |

用例与验收标准映射：
- AC① 56px + mono wordmark → PASS（boundingBox=56；fontFamily 含 mono）
- AC① active 下划线非填充 → PASS（border-bottom≥2px+brand 已上漆；背景**未**上漆）
- AC① 滚动 blur → PASS（backdrop-filter 含 blur + surface 背景上漆）
- AC② 四列 + 禁用态 → PASS（h3=4；aria-disabled=2）
- AC③ Pill/Button variants → PASS（bg-brand-dim 真填充 + brand 边框 + teal 文字；CTA primary 填充）
- AC④ 不与 T04 冲突 → PASS（13/13 回归全绿；Download→/start#install；GitHub external）
- AC⑤ check + build → PASS（build rc=0；详见上方审查小节 typecheck/lint/contrast/i18n 全绿）

### 4. 缺陷清单
无。阻断级缺陷 0，普通缺陷 0。

### 5. 回归套件位置与重跑命令
```bash
pnpm build && pnpm exec playwright test tests/e2e/issue12-t08b-visual.spec.ts
# 回归（先 next start -p 3210）：
SITE=http://localhost:3210 node tests/e2e/issue3-t04-deadlinks.mjs
BASE=http://localhost:3210 node tests/e2e/issue5-ui-tokens.mjs
```
截图证据：`test-results/issue12/{topnav-scrolled-why-active,footer,start-hero-brand-pill}.png`

### 6. 放行建议
**PASS — 建议合并回 main。** 全部验收标准经真实浏览器渲染验证通过，两套相邻回归零破坏，无阻断缺陷。

---

## 自动化测试 — ISSUE-13 / T12 关键路径 E2E（qa-automation 验证，2026-06-02）

### 1. 测试范围
独立验证 ISSUE-13（[T12] 关键路径 E2E + locale 保滚动修复）的三项验收标准：
- ① Playwright 已装（@playwright/test 1.60 + chromium 就绪）
- ② E2E 覆盖关键路径：两表单 2xx 成功卡 / 429 amber 保留态、locale EN→ZH 保滚动、CodeBlock 复制翻转 Copied、关键内链 EN+ZH 2xx
- ③ `pnpm test:e2e` 全绿

### 2. 执行环境与启动方式
- node v23.6.1 / pnpm 11.5.0（nvm）
- `pnpm build` → exit 0（生产构建成功）
- `pnpm test:e2e`（= `playwright test`）；playwright.config 自起 `next start -p 3210` 生产服务、真实 chromium 单 worker 串行驱动，测后自动回收（已确认 3210 无僵尸进程）
- 两表单 / CodeBlock 全程 page.route mock，无需 Resend/Upstash 外部密钥

### 3. 结果汇总
**全量 `pnpm test:e2e` → 66 passed (37.3s)，0 失败 / 0 跳过。** 含本工单新增 7 用例 + 既有 analytics(14)/issue4/6/10/11/12 等 59 回归，零退化。

ISSUE-13 新增 7 用例（真实浏览器执行）：
| # | 用例 | 结果 |
|---|---|---|
| 48 | cloud 表单 2xx → 持久绿色成功卡（`role=status` + "You're on the list."，`#cw-email` 消失） | ✅ 179ms |
| 49 | cloud 表单 429 → amber 温和提示（`border-amber`），表单与已填邮箱保留 | ✅ 151ms |
| 50 | design-partner 表单 2xx → 成功卡替换表单（"reply within 48 hours."） | ✅ 201ms |
| 51 | design-partner 表单 429 → amber 提示，已填 name/pain 字段保留 | ✅ 243ms |
| 52 | locale EN→ZH 切换保滚动（scrollY>600 切换后仍 >600，未跳顶） | ✅ 637ms |
| 53 | CodeBlock 复制 → 按钮翻转 "Copied" 确认态 | ✅ 148ms |
| 54 | 12 条关键内链 EN+ZH 均 2xx（含 /zh/blog 重定向收敛为 2xx） | ✅ 90ms |

### 4. 缺陷清单
无。本轮验证 0 阻断、0 缺陷。

源码改动核对：`components/locale-switcher.tsx` 仅新增 `router.replace(pathname, { locale, scroll: false })` 的 `scroll: false`（PRD D6 / UX C-1「保留滚动位置」根因修复）；用例 #52 即此修复的回归守卫，真实浏览器中滚到 1200px、切换语言后仍 >600px，证明修复生效且非测试将就。

### 5. 回归套件位置与重跑命令
- 套件：`tests/e2e/issue13-t12-critical-paths.spec.ts`（7 用例）+ `tests/e2e/` 既有 59 用例
- 重跑：`pnpm test:e2e`（全量 66）；单文件：`pnpm exec playwright test issue13-t12-critical-paths`

### 6. 放行建议
**PASS — 建议合并回 main。** 三项验收标准全部满足，生产构建 + 真实 chromium 跑通 66/66，locale 保滚动修复经 E2E 行为验证生效，无阻断缺陷。

---

## 代码审查 — ISSUE-14 [T13] CI门禁(GitHub Actions) (code-reviewer, 2026-06-02)

**结论：通过，建议放行 QA。必改 0 / 建议改 2 / 可选 1。**

审查范围：`git diff main...feature/ISSUE-14`（`.github/workflows/ci.yml` 新增、`.github/branch-protection.md` 新增、`proxy.ts` 改、`scripts/check-quickstart-sync.ts` 改）。

### 正确性（实证）
- **proxy.ts:27** matcher 加 `opengraph-image` 排除项——根级 `app/opengraph-image.tsx`（无点路由）确被原 `.*\..*` 漏掉而 404，修复正确；负向先行断言起始锚点仅命中以该串开头的根路径，locale 级 `app/[locale]/blog/[slug]/opengraph-image.tsx`（以 `blog`/`zh` 开头）不受影响。两文件均确认存在。
- **check-quickstart-sync.ts** 父 README 缺失时诚实 SKIP 跨仓项、仓内项照跑，计数口径（`ran/failed/skipped`）自洽，drift 仍 `exit 1`，无静默放水。
- **ci.yml** 五道闸独立成 step（任一红即阻断）；link-check 用 `set +e/-e`+`kill -0`+`exit $RC`，端口 3000 与 `check-links.ts` 默认一致、与 E2E `:3210` 无冲突；`cache: pnpm`/`concurrency`/`permissions: contents:read`/无不可信入参均良好；`lockfileVersion '9.0'` 与 pnpm 9 `--frozen-lockfile` 兼容。

### 发现项
| # | 位置 | 类型 | 级别 | 说明 / 建议 |
|---|------|------|------|------|
| 1 | ci.yml:99-112 + playwright.config.ts:17 | 坏味道/契约不符 | 建议改 | reporter 仅 `list` 且 `trace:off`，`playwright-report/` 永不生成 → artifact 恒空（靠 `if-no-files-found: ignore` 不报错），与"E2E报告上传"描述不符。建议 CI 加 `["html",{open:"never"}]` 以保留失败诊断物。未自动改：动 E2E 配置须 QA 复跑确认零回归。 |
| 2 | branch-protection.md:36-43 | 文档 | 建议改 | `gh api -f '...[strict]=true'` 以字符串传布尔，API 可能拒收；建议 `--input` 传 JSON 或 `-F`。属手动运维文档。 |
| 3 | ci.yml link-check 等待循环 | 风格 | 可选 | 60s 超时后 server 仍存活则不报错续跑，靠 lint:links 兜底；可加 `::error::` 提示利排障。 |

提交规范符合 Conventional Commits（`feat(ISSUE-14): ...`）。未自动修复任何项（1 触及已验证 E2E 配置交 QA，2/3 为文档/可观测性优化），均非必改、不阻断放行。

---

## 代码审查 — ISSUE-15 [T14] 内容迁MDX(blog接线) (code-reviewer, 2026-06-02)

**结论：通过，建议放行 QA。必改 0 / 建议改 1 / 可选 2。**

审查范围：`git diff main...feature/ISSUE-15`——`content/blog.ts`（原地改造）、`content/blog/*.mdx`×2（新增）、`next.config.ts`（@next/mdx 接线）、`mdx-components.tsx`（新增）、`scripts/check-blog-mdx.ts`+`blog-migration-snapshot.json`（新增守卫）、`package.json`（加 `test:blog`）。

### 实证复跑（非采信工单自述，均本机实跑）
- `pnpm typecheck` → tsc 0 错。
- `pnpm lint` → **0 error**（5 warning 全在无关文件 `components/toc.tsx`，非本次改动）。
- `pnpm test:blog` → 30/30 ✓（2 篇 ×7 字段逐字段 equal + body byte-exact + 段落数保持；provider 行为：数组/降序/已知 slug 非空 body/未知 slug undefined/getRelatedPosts limit/类型 number·string·string[]）。
- `pnpm build` → **Compiled successfully**，`● /[locale]/blog/[slug]` 静态产出 `why-parquet-for-three-signals` + `what-we-learned-from-100-incident-reviews` 两 slug（en/zh），无 MDX 相关编译告警/报错。

### 正确性 / 契约（实证）
- **契约零改（AC4）**：`git diff` 证 4 消费方（`app/[locale]/blog/page.tsx`、`blog/[slug]/page.tsx`、`app/sitemap.ts`、`blog/[slug]/opengraph-image.tsx`）**无 diff**。`@/content/blog` 仍同步导出 `BLOG_POSTS`/`getPostBySlug`/`getRelatedPosts`，`BlogPost = BlogPostMeta & { body: string }` 类型不变。
- **同步 + 模块级求值（关键约束）**：`loadPosts()` 用 `readdirSync`/`readFileSync` 同步读，模块顶层求值 `export const BLOG_POSTS`，未引入 async/Promise——`generateStaticParams()`/`sitemap()` 的同步消费安全（build 已证）。
- **排序（AC6）**：`a.date < b.date ? 1 : ...` 对 ISO `YYYY-MM-DD` 字符串降序正确，parquet(05-20) 在 incident(04-08) 前，与迁移前数组字面量顺序一致，featured 选取无漂移。
- **frontmatter 类型陷阱已规避**：`date` 加引号为 string、`readTimeMinutes` 裸数字、`tags` YAML 数组——经 `test:blog` 类型守卫 + tsc 双重确认。
- **`getRelatedPosts` 内部逻辑原样保留**，仅 `BLOG_POSTS` 数据来源由硬编码数组改为读 MDX，行为不变。
- **next.config 组合正确**：`withNextIntl(withMDX(nextConfig))` next-intl 在外层；remark 插件用字符串名 `"remark-gfm"` 规避 Turbopack「serializable options」约束；`pageExtensions` 含 `mdx` 仅影响 `app/` 路由，`content/blog/*.mdx` 在 `app/` 外不被误当页面（build 输出已证无多余路由）。

### 发现项
| # | 位置 | 类型 | 级别 | 说明 / 建议 |
|---|------|------|------|------|
| 1 | content/blog.ts:37 | 坏味道/健壮性 | 建议改 | `const meta = data as BlogPostMeta` 对 frontmatter 零校验。本工单面向「运营改 .mdx 不动代码」（AC9），若运营漏填必填字段（如 `slug`/`readTimeMinutes`），会静默产出 `slug: undefined` 的坏 post → `generateStaticParams` 路由破损，且无报错指向出错文件。建议加最小校验：缺关键字段时 `throw new Error(\`blog/${file}: missing frontmatter field ...\`)`，让 build 早失败并定位文件。非当前 bug（现 2 篇已校验有效），未自动改：属 T14 边界外的健壮性增强，且「抛错 vs 跳过」是设计取舍，留 T15/后续或交 QA 评估。 |
| 2 | content/blog.ts:46 | 风格 | 可选 | `coverUrl: meta.coverUrl` 在 frontmatter 无该字段时显式置 `undefined` 键，而原数组对象根本不含此键。无害（`JSON.stringify` 丢弃该键、渲染处 `post.coverUrl &&` 守卫），纯观感。 |
| 3 | backlog/ISSUE-14.md | 风格 | 可选 | 本次提交 `a687c01` 顺带带入 ISSUE-14 关单的 bookkeeping 改动（引擎上一周期产物），与 T14 代码无关、无害，但略不整洁；后续可注意按工单拆分提交。 |

**亮点**：`scripts/check-blog-mdx.ts` + `blog-migration-snapshot.json` 以快照逐字节对比证明「迁移零丢失」（AC3），是强力的迁移守卫工程实践，值得保留为长期回归。

提交规范符合 Conventional Commits（`feat(ISSUE-15): ...`），单提交。未自动修复任何项（1 属边界外健壮性增强含设计取舍、2/3 为可选观感/整洁），均非必改、不阻断放行。
